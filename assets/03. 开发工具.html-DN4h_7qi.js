import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{r as c,o as l,c as i,b as s,e as n,d as e,a as p}from"./app-Bmv5Ekr9.js";const o={},d=p(`<h1 id="ebpf" tabindex="-1"><a class="header-anchor" href="#ebpf"><span>eBPF</span></a></h1><h1 id="目录" tabindex="-1"><a class="header-anchor" href="#目录"><span>目录</span></a></h1><h1 id="开发工具" tabindex="-1"><a class="header-anchor" href="#开发工具"><span>开发工具</span></a></h1><p>本节讨论 BPF 周围的当前用户空间工具、内省工具和内核控制旋钮。</p><blockquote><p>!NOTE</p><p>BPF 周围的工具和基础设施仍在快速发展，因此可能无法提供所有可用工具的完整情况。</p></blockquote><h2 id="开发环境" tabindex="-1"><a class="header-anchor" href="#开发环境"><span>开发环境</span></a></h2><p>下面可以找到针对 Fedora 和 Ubuntu 设置 BPF 开发环境的分步指南。这将指导您构建、安装和测试开发内核以及构建和安装 iproute2。</p><p>手动构建 iproute2 和 Linux 内核的步骤通常是不必要的，因为默认情况下主要发行版已经发布了足够新的内核，但需要测试前沿版本或分别向 iproute2 和 Linux 内核贡献 BPF 补丁。同样，出于调试和自省的目的，构建 bpftool 是可选的，但建议这样做。</p><h3 id="fedora" tabindex="-1"><a class="header-anchor" href="#fedora"><span>Fedora</span></a></h3><p>略</p><h3 id="ubuntu" tabindex="-1"><a class="header-anchor" href="#ubuntu"><span>Ubuntu</span></a></h3><p>以下内容适用于 Ubuntu 17.04 或更高版本：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> <span class="token parameter variable">-y</span> <span class="token function">make</span> gcc libssl-dev <span class="token function">bc</span> libelf-dev libcap-dev <span class="token punctuation">\\</span>
  clang gcc-multilib llvm libncurses5-dev <span class="token function">git</span> pkg-config libmnl-dev bison flex <span class="token punctuation">\\</span>
  graphviz
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="opensuse-tumbleweed" tabindex="-1"><a class="header-anchor" href="#opensuse-tumbleweed"><span>OpenSUSE Tumbleweed</span></a></h3><p>略</p><h2 id="编译内核" tabindex="-1"><a class="header-anchor" href="#编译内核"><span>编译内核</span></a></h2><p>Linux 内核新 BPF 功能的开发发生在 <code>net-next</code> git 树中，最新的 BPF 修复位于 <code>net</code> 树中。以下命令将通过 git 获取 <code>net-next</code> 树的内核源代码：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> clone git://git.kernel.org/pub/scm/linux/kernel/git/netdev/net-next.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果 git 提交历史记录不感兴趣，那么 <code>--depth 1</code> 将通过仅将 git 历史记录截断到最近的提交来更快地克隆树。</p><p>如果您对 <code>net</code> 树感兴趣，可以从此 url 克隆它：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> clone git://git.kernel.org/pub/scm/linux/kernel/git/netdev/net.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>互联网上有几十个关于如何构建 Linux 内核的教程，一个很好的资源是 Kernel Newbies 网站 (https://kernelnewbies.org/KernelBuild)，可以使用上面提到的两个 git 树之一进行操作。</p><p>确保生成的 <code>.config</code> 文件包含以下用于运行 BPF 的 <code>CONFIG_*</code> 条目。 Cilium 也需要这些条目。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token assign-left variable">CONFIG_CGROUP_BPF</span><span class="token operator">=</span>y
<span class="token assign-left variable">CONFIG_BPF</span><span class="token operator">=</span>y
<span class="token assign-left variable">CONFIG_BPF_SYSCALL</span><span class="token operator">=</span>y
<span class="token assign-left variable">CONFIG_NET_SCH_INGRESS</span><span class="token operator">=</span>m
<span class="token assign-left variable">CONFIG_NET_CLS_BPF</span><span class="token operator">=</span>m
<span class="token assign-left variable">CONFIG_NET_CLS_ACT</span><span class="token operator">=</span>y
<span class="token assign-left variable">CONFIG_BPF_JIT</span><span class="token operator">=</span>y
<span class="token assign-left variable">CONFIG_LWTUNNEL_BPF</span><span class="token operator">=</span>y
<span class="token assign-left variable">CONFIG_HAVE_EBPF_JIT</span><span class="token operator">=</span>y
<span class="token assign-left variable">CONFIG_BPF_EVENTS</span><span class="token operator">=</span>y
<span class="token assign-left variable">CONFIG_TEST_BPF</span><span class="token operator">=</span>m
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>某些条目无法通过 <code>make menuconfig</code> 进行调整。例如，如果给定架构确实带有 eBPF JIT，则会自动选择 <code>CONFIG_HAVE_EBPF_JIT</code> 。在这种特定情况下， <code>CONFIG_HAVE_EBPF_JIT</code> 是可选的，但强烈建议使用。没有 eBPF JIT 编译器的架构将需要回退到内核解释器，但代价是执行 BPF 指令的效率较低。</p><h3 id="验证设置" tabindex="-1"><a class="header-anchor" href="#验证设置"><span>验证设置</span></a></h3><p>启动到新编译的内核后，导航到 BPF 自测试套件以测试 BPF 功能（当前工作目录指向克隆的 git 树的根目录）：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token builtin class-name">cd</span> tools/testing/selftests/bpf/
$ <span class="token function">make</span>
$ <span class="token function">sudo</span> ./test_verifier
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>验证程序测试打印出当前正在执行的所有检查。运行所有测试结束时的摘要将转储测试成功和失败的信息：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ Summary: <span class="token number">847</span> PASSED, <span class="token number">0</span> SKIPPED, <span class="token number">0</span> FAILED
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,30),r=s("p",null,"[!NOTE]",-1),u={href:"https://docs.cilium.io/en/stable/bpf/architecture/#bpf-to-bpf-calls",target:"_blank",rel:"noopener noreferrer"},m={href:"https://docs.cilium.io/en/stable/bpf/toolchain/#tooling-llvm",target:"_blank",rel:"noopener noreferrer"},v=p(`<p>为了运行所有 BPF 自测试，需要以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">sudo</span> <span class="token function">make</span> run_tests
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="编译-iproute2" tabindex="-1"><a class="header-anchor" href="#编译-iproute2"><span>编译 iproute2</span></a></h3><p>与 <code>net</code> （仅修复）和 <code>net-next</code> （新功能）内核树类似，iproute2 git 树有两个分支，即 <code>master</code> 和 <code>net-next</code> 分支基于 <code>net</code> 树， <code>net-next</code> 分支基于 <code>net-next</code> 内核树。这是必要的，以便头文件的更改可以在 iproute2 树中同步。</p><p>为了克隆 iproute2 <code>master</code> 分支，可以使用以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> clone https://git.kernel.org/pub/scm/network/iproute2/iproute2.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>同样，要克隆到 iproute2 的上述 <code>net-next</code> 分支，请运行以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> clone <span class="token parameter variable">-b</span> net-next https://git.kernel.org/pub/scm/network/iproute2/iproute2.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>之后，继续构建和安装：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token builtin class-name">cd</span> iproute2/
$ ./configure <span class="token parameter variable">--prefix</span><span class="token operator">=</span>/usr
TC schedulers
 ATM    no

libc has setns: <span class="token function">yes</span>
SELinux support: <span class="token function">yes</span>
ELF support: <span class="token function">yes</span>
libmnl support: no
Berkeley DB: no

docs: latex: no
 WARNING: no docs can be built from LaTeX files
 sgml2html: no
 WARNING: no HTML docs can be built from SGML
$ <span class="token function">make</span>
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
$ <span class="token function">sudo</span> <span class="token function">make</span> <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>确保 <code>configure</code> 脚本显示 <code>ELF support: yes</code> ，以便 iproute2 可以处理来自 LLVM 的 BPF 后端的 ELF 文件。 libelf 已在之前的 Fedora 和 Ubuntu 的依赖项安装说明中列出。</p><h3 id="编译-bpftool" tabindex="-1"><a class="header-anchor" href="#编译-bpftool"><span>编译 bpftool</span></a></h3><p>bpftool 是 BPF 程序和映射的调试和内省的重要工具。它是内核树的一部分，可在 <code>tools/bpf/bpftool/</code> 下找到。</p><p>确保如前所述克隆了 <code>net</code> 或 <code>net-next</code> 内核树。为了构建和安装 bpftool，需要执行以下步骤：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token builtin class-name">cd</span> <span class="token operator">&lt;</span>kernel-tree<span class="token operator">&gt;</span>/tools/bpf/bpftool/
$ <span class="token function">make</span>
Auto-detecting system features:
<span class="token punctuation">..</span>.                        libbfd: <span class="token punctuation">[</span> on  <span class="token punctuation">]</span>
<span class="token punctuation">..</span>.        disassembler-four-args: <span class="token punctuation">[</span> OFF <span class="token punctuation">]</span>

  CC       xlated_dumper.o
  CC       prog.o
  CC       common.o
  CC       cgroup.o
  CC       main.o
  CC       json_writer.o
  CC       cfg.o
  CC       map.o
  CC       jit_disasm.o
  CC       disasm.o
make<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span>: Entering directory <span class="token string">&#39;/home/foo/trees/net/tools/lib/bpf&#39;</span>

Auto-detecting system features:
<span class="token punctuation">..</span>.                        libelf: <span class="token punctuation">[</span> on  <span class="token punctuation">]</span>
<span class="token punctuation">..</span>.                           bpf: <span class="token punctuation">[</span> on  <span class="token punctuation">]</span>

  CC       libbpf.o
  CC       bpf.o
  CC       nlattr.o
  LD       libbpf-in.o
  LINK     libbpf.a
make<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span>: Leaving directory <span class="token string">&#39;/home/foo/trees/bpf/tools/lib/bpf&#39;</span>
  LINK     bpftool
$ <span class="token function">sudo</span> <span class="token function">make</span> <span class="token function">install</span>

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="llvm" tabindex="-1"><a class="header-anchor" href="#llvm"><span>LLVM</span></a></h2><h3 id="llvm-1" tabindex="-1"><a class="header-anchor" href="#llvm-1"><span>LLVM</span></a></h3><p>LLVM 是目前唯一提供 BPF 后端的编译器套件。 gcc 目前不支持 BPF。</p><p>BPF 后端已合并到 LLVM 的 3.7 版本中。主要发行版在打包 LLVM 时默认启用 BPF 后端，因此在最新发行版上安装 clang 和 llvm 足以开始将 C 编译成 BPF 目标文件。</p><p>典型的工作流程是BPF程序用C语言编写，由LLVM编译成对象/ELF文件，由用户空间BPF ELF加载器（例如iproute2或其他）解析，并通过BPF系统调用推送到内核中。内核验证 BPF 指令并对它们进行 JIT，返回程序的新文件描述符，然后可以将其附加到子系统（例如网络）。如果支持，子系统可以进一步将 BPF 程序卸载到硬件（例如 NIC）。</p><p>对于 LLVM，可以通过以下方式检查 BPF 目标支持：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ llc <span class="token parameter variable">--version</span>
LLVM <span class="token punctuation">(</span>http://llvm.org/<span class="token punctuation">)</span>:
LLVM version <span class="token number">3.8</span>.1
Optimized build.
Default target: x86_64-unknown-linux-gnu
Host CPU: skylake

Registered Targets:
  <span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
  bpf        - BPF <span class="token punctuation">(</span>host endian<span class="token punctuation">)</span>
  bpfeb      - BPF <span class="token punctuation">(</span>big endian<span class="token punctuation">)</span>
  bpfel      - BPF <span class="token punctuation">(</span>little endian<span class="token punctuation">)</span>
  <span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>默认情况下， <code>bpf</code> 目标使用其编译时CPU的字节序，这意味着如果CPU的字节序是小字节序，则程序也以小字节序格式表示，如果CPU的字节序是大字节序字节序，程序以大字节序表示。这也与 BPF 的运行时行为相匹配，BPF 是通用的，并使用其运行所在的 CPU 的字节序，以免对任何格式的架构造成不利影响。</p><p>对于交叉编译，引入了两个目标 <code>bpfeb</code> 和 <code>bpfel</code> ，因为 BPF 程序可以在以一种字节序运行的节点上编译（例如 x86 上的小字节序）并运行在另一种字节序格式的节点上（例如，arm 上的大字节序）。请注意，前端 (clang) 也需要以目标字节序运行。</p><p>在不适用混合字节序的情况下，使用 <code>bpf</code> 作为目标是首选方式。例如，由于 <code>x86_64</code> 上的编译会导致目标 <code>bpf</code> 和 <code>bpfel</code> 的输出相同，因为它们是小尾数，因此触发编译的脚本也没有具有字节序意识。</p><p>最小的独立 XDP 删除程序可能类似于以下示例 ( <code>xdp-example.c</code> )：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#include &lt;linux/bpf.h&gt;</span>

<span class="token comment">#ifndef __section</span>
<span class="token comment"># define __section(NAME)                  \\</span>
   __attribute__<span class="token variable"><span class="token punctuation">((</span>section<span class="token punctuation">(</span>NAME<span class="token punctuation">)</span><span class="token punctuation">,</span> used<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

__section<span class="token punctuation">(</span><span class="token string">&quot;prog&quot;</span><span class="token punctuation">)</span>
int xdp_drop<span class="token punctuation">(</span>struct xdp_md *ctx<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    <span class="token builtin class-name">return</span> XDP_DROP<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

char __license<span class="token punctuation">[</span><span class="token punctuation">]</span> __section<span class="token punctuation">(</span><span class="token string">&quot;license&quot;</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token string">&quot;GPL&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>然后可以将其编译并加载到内核中，如下所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">-O2</span> <span class="token parameter variable">-Wall</span> <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> xdp-example.o
<span class="token comment"># ip link set dev em1 xdp obj xdp-example.o</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>[!NOTE]</p><p>如上所述将 XDP BPF 程序附加到网络设备需要 Linux 4.11 以及支持 XDP 的设备，或者 Linux 4.12 或更高版本。</p></blockquote><p>对于生成的目标文件，LLVM (&gt;= 3.9) 使用官方 BPF 机器值，即 <code>EM_BPF</code> （十进制： <code>247</code> / 十六进制： <code>0xf7</code> ）。在此示例中，程序已使用 <code>x86_64</code> 下的 <code>bpf</code> 目标进行编译，因此显示 <code>LSB</code> （与 <code>MSB</code> 相对）字节序：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">file</span> xdp-example.o
xdp-example.o: ELF <span class="token number">64</span>-bit LSB relocatable, *unknown arch 0xf7* version <span class="token number">1</span> <span class="token punctuation">(</span>SYSV<span class="token punctuation">)</span>, not stripped
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>readelf -a xdp-example.o</code> 将转储有关 ELF 文件的更多信息，这有时对于内省生成的节头、重定位条目和符号表很有用。</p><p>在不太可能的情况下，需要从头开始编译 clang 和 LLVM，可以使用以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> clone https://github.com/llvm/llvm-project.git
$ <span class="token builtin class-name">cd</span> llvm-project
$ <span class="token function">mkdir</span> build
$ <span class="token builtin class-name">cd</span> build
$ cmake <span class="token parameter variable">-DLLVM_ENABLE_PROJECTS</span><span class="token operator">=</span>clang <span class="token parameter variable">-DLLVM_TARGETS_TO_BUILD</span><span class="token operator">=</span><span class="token string">&quot;BPF;X86&quot;</span> <span class="token parameter variable">-DBUILD_SHARED_LIBS</span><span class="token operator">=</span>OFF <span class="token parameter variable">-DCMAKE_BUILD_TYPE</span><span class="token operator">=</span>Release <span class="token parameter variable">-DLLVM_BUILD_RUNTIME</span><span class="token operator">=</span>OFF  <span class="token parameter variable">-G</span> <span class="token string">&quot;Unix Makefiles&quot;</span> <span class="token punctuation">..</span>/llvm
$ <span class="token function">make</span> <span class="token parameter variable">-j</span> <span class="token variable"><span class="token variable">$(</span>getconf _NPROCESSORS_ONLN<span class="token variable">)</span></span>
$ ./bin/llc <span class="token parameter variable">--version</span>
LLVM <span class="token punctuation">(</span>http://llvm.org/<span class="token punctuation">)</span>:
LLVM version x.y.zsvn
Optimized build.
Default target: x86_64-unknown-linux-gnu
Host CPU: skylake

Registered Targets:
  bpf    - BPF <span class="token punctuation">(</span>host endian<span class="token punctuation">)</span>
  bpfeb  - BPF <span class="token punctuation">(</span>big endian<span class="token punctuation">)</span>
  bpfel  - BPF <span class="token punctuation">(</span>little endian<span class="token punctuation">)</span>
  x86    - <span class="token number">32</span>-bit X86: Pentium-Pro and above
  x86-64 - <span class="token number">64</span>-bit X86: EM64T and AMD64

$ <span class="token builtin class-name">export</span> <span class="token assign-left variable"><span class="token environment constant">PATH</span></span><span class="token operator">=</span><span class="token environment constant">$PWD</span>/bin:<span class="token environment constant">$PATH</span>   <span class="token comment"># add to ~/.bashrc</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>确保 <code>--version</code> 提及 <code>Optimized build.</code> ，否则当 LLVM 处于调试模式时，程序的编译时间将显着增加（例如增加 10 倍或更多）</p><p>为了调试，clang 可以生成汇编器输出，如下所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">-O2</span> <span class="token parameter variable">-S</span> <span class="token parameter variable">-Wall</span> <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> xdp-example.S
$ <span class="token function">cat</span> xdp-example.S
    .text
    .section    prog,<span class="token string">&quot;ax&quot;</span>,@progbits
    .globl      xdp_drop
    .p2align    <span class="token number">3</span>
xdp_drop:                             <span class="token comment"># @xdp_drop</span>
<span class="token comment"># BB#0:</span>
    r0 <span class="token operator">=</span> <span class="token number">1</span>
    <span class="token builtin class-name">exit</span>

    .section    license,<span class="token string">&quot;aw&quot;</span>,@progbits
    .globl    __license               <span class="token comment"># @__license</span>
__license:
    .asciz    <span class="token string">&quot;GPL&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>从 LLVM 6.0 版本开始，还支持汇编器解析器。您可以直接使用BPF汇编器进行编程，然后使用llvm-mc将其汇编成目标文件。例如，您可以使用以下命令将上面列出的 xdp-example.S 组装回目标文件：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ llvm-mc <span class="token parameter variable">-triple</span> bpf <span class="token parameter variable">-filetype</span><span class="token operator">=</span>obj <span class="token parameter variable">-o</span> xdp-example.o xdp-example.S
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>此外，较新的 LLVM 版本（&gt;= 4.0）还可以将 dwarf 格式的调试信息存储到目标文件中。这可以通过通常的工作流程添加 <code>-g</code> 进行编译来完成。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">-O2</span> <span class="token parameter variable">-g</span> <span class="token parameter variable">-Wall</span> <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> xdp-example.o
$ llvm-objdump <span class="token parameter variable">-S</span> --no-show-raw-insn xdp-example.o

xdp-example.o:        <span class="token function">file</span> <span class="token function">format</span> ELF64-BPF

Disassembly of section prog:
xdp_drop:
<span class="token punctuation">;</span> <span class="token punctuation">{</span>
    <span class="token number">0</span>:        r0 <span class="token operator">=</span> <span class="token number">1</span>
<span class="token punctuation">;</span> <span class="token builtin class-name">return</span> XDP_DROP<span class="token punctuation">;</span>
    <span class="token number">1</span>:        <span class="token builtin class-name">exit</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>然后， <code>llvm-objdump</code> 工具可以使用编译中使用的原始 C 代码来注释汇编器输出。本例中的简单示例不包含太多 C 代码，但是，显示为 <code>0:</code> 和 <code>1:</code> 的行号直接对应于内核的验证程序日志。</p><p>这意味着，如果 BPF 程序被验证者拒绝， <code>llvm-objdump</code> 可以帮助将指令关联回原始 C 代码，这对于分析非常有用。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> dev em1 xdp obj xdp-example.o verb

Prog section <span class="token string">&#39;prog&#39;</span> loaded <span class="token punctuation">(</span><span class="token number">5</span><span class="token punctuation">)</span><span class="token operator">!</span>
 - Type:         <span class="token number">6</span>
 - Instructions: <span class="token number">2</span> <span class="token punctuation">(</span><span class="token number">0</span> over limit<span class="token punctuation">)</span>
 - License:      GPL

Verifier analysis:

<span class="token number">0</span>: <span class="token punctuation">(</span>b7<span class="token punctuation">)</span> r0 <span class="token operator">=</span> <span class="token number">1</span>
<span class="token number">1</span>: <span class="token punctuation">(</span><span class="token number">95</span><span class="token punctuation">)</span> <span class="token builtin class-name">exit</span>
processed <span class="token number">2</span> insns

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>从验证器分析中可以看出， <code>llvm-objdump</code> 输出转储与内核相同的 BPF 汇编代码。</p><p>省略 <code>--no-show-raw-insn</code> 选项也会将原始 <code>struct bpf_insn</code> 转储为程序集前面的十六进制：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ llvm-objdump <span class="token parameter variable">-S</span> xdp-example.o

xdp-example.o:        <span class="token function">file</span> <span class="token function">format</span> ELF64-BPF

Disassembly of section prog:
xdp_drop:
<span class="token punctuation">;</span> <span class="token punctuation">{</span>
   <span class="token number">0</span>:       b7 00 00 00 01 00 00 00     r0 <span class="token operator">=</span> <span class="token number">1</span>
<span class="token punctuation">;</span> <span class="token builtin class-name">return</span> foo<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
   <span class="token number">1</span>:       <span class="token number">95</span> 00 00 00 00 00 00 00     <span class="token builtin class-name">exit</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>对于 LLVM IR 调试，BPF 的编译过程可以分为两个步骤，生成一个二进制 LLVM IR 中间文件 <code>xdp-example.bc</code> ，稍后可以将其传递给 llc：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">-O2</span> <span class="token parameter variable">-Wall</span> <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf -emit-llvm <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> xdp-example.bc
$ llc xdp-example.bc <span class="token parameter variable">-march</span><span class="token operator">=</span>bpf <span class="token parameter variable">-filetype</span><span class="token operator">=</span>obj <span class="token parameter variable">-o</span> xdp-example.o
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>生成的 LLVM IR 还可以通过以下方式转储为人类可读的格式：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>clang <span class="token parameter variable">-O2</span> <span class="token parameter variable">-Wall</span> -emit-llvm <span class="token parameter variable">-S</span> <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> -
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>LLVM 能够将调试信息（例如程序中使用的数据类型的描述）附加到生成的 BPF 目标文件中。默认情况下，这是 DWARF 格式。</p><p>BPF 使用的一个高度简化的版本称为 BTF（BPF 类型格式）。生成的 DWARF 可以转换为 BTF，然后通过 BPF 对象加载器加载到内核中。然后，内核将验证 BTF 数据的正确性并跟踪 BTF 数据包含的数据类型。</p><p>然后，可以使用 BTF 数据中的键和值类型对 BPF 映射进行注释，以便稍后的映射转储会导出映射数据以及相关类型信息。这允许更好的自省、调试和价值漂亮的打印。请注意，BTF 数据是通用调试数据格式，因此可以加载任何 DWARF 到 BTF 转换的数据（例如，内核的 vmlinux DWARF 数据可以转换为 BTF 并加载）。后者对于未来的 BPF 跟踪特别有用。</p><p>为了从 DWARF 调试信息生成 BTF，需要 elfutils (&gt;= 0.173)。如果该选项不可用，则需要在编译期间将 <code>-mattr=dwarfris</code> 选项添加到 <code>llc</code> 命令中：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ llc <span class="token parameter variable">-march</span><span class="token operator">=</span>bpf <span class="token parameter variable">-mattr</span><span class="token operator">=</span>help <span class="token operator">|&amp;</span> <span class="token function">grep</span> dwarfris
  dwarfris - Disable MCAsmInfo DwarfUsesRelocationsAcrossSections.
  <span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>使用 <code>-mattr=dwarfris</code> 的原因是因为标志 <code>dwarfris</code> ( <code>dwarf relocation in section</code> ) 禁用了 DWARF 和 ELF 符号表之间的 DWARF 横截面重定位，因为 libdw 没有适当的 BPF重定位支持，因此像 <code>pahole</code> 这样的工具将无法正确地从对象中转储结构。</p><p>elfutils (&gt;= 0.173) 实现了正确的 BPF 重定位支持，因此无需 <code>-mattr=dwarfris</code> 选项也可以实现相同的效果。从目标文件中转储结构可以通过 DWARF 或 BTF 信息来完成。 <code>pahole</code> 此时使用 LLVM 发出的 DWARF 信息，但是，未来的 <code>pahole</code> 版本可能依赖 BTF（如果可用）。</p><p>要将 DWARF 转换为 BTF，需要最新的 pahole 版本 (&gt;= 1.12)。如果某个发行包中没有提供最新的 pahole 版本，也可以从其官方 git 存储库获取：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> clone https://git.kernel.org/pub/scm/devel/pahole/pahole.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><code>pahole</code> 附带选项 <code>-J</code> 将 DWARF 从目标文件转换为 BTF。可以按如下方式探测 <code>pahole</code> 是否支持 BTF（请注意， <code>pahole</code> 也需要 <code>llvm-objcopy</code> 工具，因此还要检查它是否存在）：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ pahole <span class="token parameter variable">--help</span> <span class="token operator">|</span> <span class="token function">grep</span> BTF
-J, <span class="token parameter variable">--btf_encode</span>           Encode as BTF
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>生成调试信息还需要前端通过将 <code>-g</code> 传递到 <code>clang</code> 命令行来生成源代码级调试信息。请注意，无论是否使用 <code>llc</code> 的 <code>dwarfris</code> 选项，都需要 <code>-g</code> 。生成目标文件的完整示例：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">-O2</span> <span class="token parameter variable">-g</span> <span class="token parameter variable">-Wall</span> <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf -emit-llvm <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> xdp-example.bc
$ llc xdp-example.bc <span class="token parameter variable">-march</span><span class="token operator">=</span>bpf <span class="token parameter variable">-mattr</span><span class="token operator">=</span>dwarfris <span class="token parameter variable">-filetype</span><span class="token operator">=</span>obj <span class="token parameter variable">-o</span> xdp-example.o
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>或者，通过使用clang仅构建带有调试信息的BPF程序(同样，当具有适当的有用版本时，可以省略dwarfris标志):</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf <span class="token parameter variable">-O2</span> <span class="token parameter variable">-g</span> <span class="token parameter variable">-c</span> <span class="token parameter variable">-Xclang</span> -target-feature <span class="token parameter variable">-Xclang</span> +dwarfris <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> xdp-example.o
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>编译成功后，可以使用 <code>pahole</code> 根据DWARF信息正确转储BPF程序的结构：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ pahole xdp-example.o
struct xdp_md <span class="token punctuation">{</span>
        __u32                      data<span class="token punctuation">;</span>                 /*     <span class="token number">0</span>     <span class="token number">4</span> */
        __u32                      data_end<span class="token punctuation">;</span>             /*     <span class="token number">4</span>     <span class="token number">4</span> */
        __u32                      data_meta<span class="token punctuation">;</span>            /*     <span class="token number">8</span>     <span class="token number">4</span> */

        /* size: <span class="token number">12</span>, cachelines: <span class="token number">1</span>, members: <span class="token number">3</span> */
        /* last cacheline: <span class="token number">12</span> bytes */
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>通过选项 <code>-J</code> <code>pahole</code> 最终可以从DWARF生成BTF。在目标文件中，DWARF 数据仍将与新添加的 BTF 数据一起保留。完整的 <code>clang</code> 和 <code>pahole</code> 示例组合：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf <span class="token parameter variable">-O2</span> <span class="token parameter variable">-Wall</span> <span class="token parameter variable">-g</span> <span class="token parameter variable">-c</span> <span class="token parameter variable">-Xclang</span> -target-feature <span class="token parameter variable">-Xclang</span> +dwarfris <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> xdp-example.o
$ pahole <span class="token parameter variable">-J</span> xdp-example.o
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>通过 <code>readelf</code> 工具可以看到 <code>.BTF</code> 部分的存在：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ readelf <span class="token parameter variable">-a</span> xdp-example.o
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
  <span class="token punctuation">[</span><span class="token number">18</span><span class="token punctuation">]</span> .BTF              PROGBITS         0000000000000000  00000671
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>BPF 加载器（例如 iproute2）将检测并加载 BTF 部分，以便 BPF 映射可以用类型信息进行注释。</p><p>LLVM 默认使用 BPF 基本指令集来生成代码，以确保生成的目标文件也可以加载较旧的内核，例如长期稳定的内核（例如 4.9+）。</p><p>然而，LLVM 在 BPF 后端有一个 <code>-mcpu</code> 选择器，以便选择不同版本的 BPF 指令集，即在 BPF 基本指令集之上的指令集扩展，以便生成更高效、更小的指令集。代码。</p><p>可用的 <code>-mcpu</code> 选项可以通过以下方式查询：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ llc <span class="token parameter variable">-march</span> bpf <span class="token parameter variable">-mcpu</span><span class="token operator">=</span>help
Available CPUs <span class="token keyword">for</span> this target:

  generic - Select the generic processor.
  probe   - Select the probe processor.
  v1      - Select the v1 processor.
  v2      - Select the v2 processor.
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>generic</code> 处理器是默认处理器，也是BPF的基本指令集 <code>v1</code> 。选项 <code>v1</code> 和 <code>v2</code> 通常在交叉编译 BPF 程序且加载程序的目标主机与编译程序的目标主机不同的环境中很有用（因此可用的 BPF 内核功能也可能有所不同）。</p><p>推荐的 <code>-mcpu</code> 选项也是 Cilium 内部使用的 <code>-mcpu=probe</code> ！这里，LLVM BPF 后端查询内核是否有 BPF 指令集扩展的可用性，当发现可用时，LLVM 将在适当的时候使用它们来编译 BPF 程序。</p><p>带有 llc 的 <code>-mcpu=probe</code> 的完整命令行示例：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">-O2</span> <span class="token parameter variable">-Wall</span> <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf -emit-llvm <span class="token parameter variable">-c</span> xdp-example.c <span class="token parameter variable">-o</span> xdp-example.bc
$ llc xdp-example.bc <span class="token parameter variable">-march</span><span class="token operator">=</span>bpf <span class="token parameter variable">-mcpu</span><span class="token operator">=</span>probe <span class="token parameter variable">-filetype</span><span class="token operator">=</span>obj <span class="token parameter variable">-o</span> xdp-example.o
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>一般来说，LLVM IR 生成是独立于体系结构的。然而，使用 <code>clang --target=bpf</code> 与保留 <code>--target=bpf</code> 并因此使用 clang 的默认目标（根据底层架构的不同，可能是 <code>x86_64</code> 、 &lt; b3&gt; 或其他。</p><p>引用内核的 <code>Documentation/bpf/bpf_devel_QA.txt</code> ：</p><ul><li>BPF 程序可以递归地包含具有文件范围内联汇编代码的头文件。默认目标可以很好地处理这个问题，而如果 bpf 后端汇编器不理解这些汇编代码，则 bpf 目标可能会失败，这在大多数情况下都是如此。</li><li>当不使用 -g 进行编译时，其他 elf 部分（例如 <code>.eh_frame</code> 和 <code>.rela.eh_frame</code> ）可能会出现在具有默认目标的目标文件中，但不会出现在 bpf 目标中。</li><li>默认目标可以将C switch 语句转换为switch 表查找和跳转操作。由于开关表被放置在全局只读部分，因此bpf程序将无法加载。 bpf 目标不支持切换表优化。 clang 选项 <code>-fno-jump-tables</code> 可用于禁用开关表生成。</li><li>对于 clang <code>--target=bpf</code> ，保证指针或 long / unsigned long 类型的宽度始终为 64 位，无论底层 clang 二进制文件或默认目标（或内核）是否为 32 位。但是，当使用本机 clang 目标时，它将根据底层架构的约定来编译这些类型，这意味着在 32 位架构、指针或 long/unsigned long 类型的情况下，例如在 BPF 上下文结构中，宽度为 32 位，而 BPF LLVM 后端仍以 64 位运行。</li></ul><p>在跟踪映射 CPU 寄存器的内核 <code>struct pt_regs</code> 或 CPU 寄存器宽度很重要的其他内核结构的情况下，主要需要本机目标。在所有其他情况（例如网络）中，使用 <code>clang --target=bpf</code> 是首选。</p><p>此外，从 LLVM 7.0 版本开始，LLVM 开始支持 32 位子寄存器和 BPF ALU32 指令。添加了新的代码生成属性 <code>alu32</code> 。启用后，LLVM 将尽可能尝试使用 32 位子寄存器，通常是在对 32 位类型进行操作时。具有 32 位子寄存器的关联 ALU 指令将成为 ALU32 指令。例如，对于以下示例代码：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">cat</span> <span class="token number">32</span>-bit-example.c
    void cal<span class="token punctuation">(</span>unsigned int *a, unsigned int *b, unsigned int *c<span class="token punctuation">)</span>
    <span class="token punctuation">{</span>
      unsigned int <span class="token function">sum</span> <span class="token operator">=</span> *a + *b<span class="token punctuation">;</span>
      *c <span class="token operator">=</span> <span class="token function">sum</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在默认代码生成时，汇编器将如下所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf -emit-llvm <span class="token parameter variable">-S</span> <span class="token number">32</span>-bit-example.c
$ llc <span class="token parameter variable">-march</span><span class="token operator">=</span>bpf <span class="token number">32</span>-bit-example.ll
$ <span class="token function">cat</span> <span class="token number">32</span>-bit-example.s
    cal:
      r1 <span class="token operator">=</span> *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r1 + <span class="token number">0</span><span class="token punctuation">)</span>
      r2 <span class="token operator">=</span> *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r2 + <span class="token number">0</span><span class="token punctuation">)</span>
      r2 <span class="token operator">+=</span> r1
      *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r3 + <span class="token number">0</span><span class="token punctuation">)</span> <span class="token operator">=</span> r2
      <span class="token builtin class-name">exit</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>使用64位寄存器，因此加法意味着64位加法。现在，如果您通过指定 <code>-mattr=+alu32</code> 启用新的 32 位子寄存器支持，则汇编器将如下所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ llc <span class="token parameter variable">-march</span><span class="token operator">=</span>bpf <span class="token parameter variable">-mattr</span><span class="token operator">=</span>+alu32 <span class="token number">32</span>-bit-example.ll
$ <span class="token function">cat</span> <span class="token number">32</span>-bit-example.s
    cal:
      w1 <span class="token operator">=</span> *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r1 + <span class="token number">0</span><span class="token punctuation">)</span>
      w2 <span class="token operator">=</span> *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r2 + <span class="token number">0</span><span class="token punctuation">)</span>
      w2 <span class="token operator">+=</span> w1
      *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r3 + <span class="token number">0</span><span class="token punctuation">)</span> <span class="token operator">=</span> w2
      <span class="token builtin class-name">exit</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>w</code> 寄存器，即32位子寄存器，将用来代替64位 <code>r</code> 寄存器。</p><p>启用 32 位子寄存器可能有助于减少类型扩展指令序列。它还可以帮助用于 32 位架构的内核 eBPF JIT 编译器，其中寄存器对用于对 64 位 eBPF 寄存器进行建模，并且需要额外的指令来操作高 32 位。给定从 32 位子寄存器读取保证只从低 32 位读取，即使写入仍然需要清除高 32 位，如果 JIT 编译器已知一个寄存器的定义只有子寄存器读取，则设置指令目标的高 32 位可以被消除。</p><h3 id="bpf-模型的一些差异" tabindex="-1"><a class="header-anchor" href="#bpf-模型的一些差异"><span>BPF 模型的一些差异</span></a></h3><p>与使用 C 进行的常规应用程序开发相比，为 BPF 编写 C 程序时需要注意一些陷阱。以下各项描述了 <strong>BPF 模型的一些差异</strong>：</p><h4 id="一切都需要内联-没有可用的函数调用-在旧的-llvm-版本上-或共享库调用" tabindex="-1"><a class="header-anchor" href="#一切都需要内联-没有可用的函数调用-在旧的-llvm-版本上-或共享库调用"><span>一切都需要内联，没有可用的函数调用（在旧的 LLVM 版本上）或共享库调用</span></a></h4><p>共享库等不能与 BPF 一起使用。但是，BPF 程序中使用的公共库代码可以放入头文件中并包含在主程序中。例如，Cilium 大量使用它（参见 <code>bpf/lib/</code> ）。但是，这仍然允许包含头文件，例如来自内核或其他库的头文件，并重用它们的静态内联函数或宏/定义。</p><p>除非使用支持 BPF 到 BPF 函数调用的最新内核 (4.16+) 和 LLVM (6.0+)，否则 LLVM 需要将整个代码编译并内联到给定程序部分的 BPF 指令的平面序列中。在这种情况下，最佳实践是对每个库函数使用类似 <code>__inline</code> 的注释，如下所示。建议使用 <code>always_inline</code> ，因为编译器仍然可以决定取消内联仅注释为 <code>inline</code> 的大型函数。</p><p>如果发生后者，LLVM 将在 ELF 文件中生成一个重定位条目，而 BPF ELF 加载程序（例如 iproute2）无法解析该重定位条目，因此会产生错误，因为只有 BPF 映射才是加载程序可以处理的有效重定位条目。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#include &lt;linux/bpf.h&gt;</span>

<span class="token comment">#ifndef __section</span>
<span class="token comment"># define __section(NAME)                  \\</span>
   __attribute__<span class="token variable"><span class="token punctuation">((</span>section<span class="token punctuation">(</span>NAME<span class="token punctuation">)</span><span class="token punctuation">,</span> used<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef __inline</span>
<span class="token comment"># define __inline                         \\</span>
   inline __attribute__<span class="token variable"><span class="token punctuation">((</span>always_inline<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

static __inline int foo<span class="token punctuation">(</span>void<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    <span class="token builtin class-name">return</span> XDP_DROP<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

__section<span class="token punctuation">(</span><span class="token string">&quot;prog&quot;</span><span class="token punctuation">)</span>
int xdp_drop<span class="token punctuation">(</span>struct xdp_md *ctx<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    <span class="token builtin class-name">return</span> foo<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

char __license<span class="token punctuation">[</span><span class="token punctuation">]</span> __section<span class="token punctuation">(</span><span class="token string">&quot;license&quot;</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token string">&quot;GPL&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="多个程序可以驻留在单个-c-文件的不同部分中" tabindex="-1"><a class="header-anchor" href="#多个程序可以驻留在单个-c-文件的不同部分中"><span>多个程序可以驻留在单个 C 文件的不同部分中</span></a></h4><p>BPF 的 C 程序大量使用节注释。 C 文件通常分为 3 个或更多部分。 BPF ELF 加载器使用这些名称来提取和准备相关信息，以便通过 bpf 系统调用加载程序和映射。例如，iproute2 使用 <code>maps</code> 和 <code>license</code> 作为默认部分名称，分别查找创建地图所需的元数据和 BPF 程序的许可证。在程序创建时，后者也被推送到内核中，并启用一些仅在程序也持有 GPL 兼容许可证的情况下公开为 GPL 的辅助函数，例如 <code>bpf_ktime_get_ns()</code> 、 <code>bpf_probe_read()</code> 和其他。</p><p>其余部分名称特定于 BPF 程序代码，例如，以下代码已修改为包含两个程序部分 <code>ingress</code> 和 <code>egress</code> 。玩具示例代码演示了两者可以共享映射和公共静态内联帮助器，例如 <code>account_data()</code> 函数。</p><p><code>xdp-example.c</code> 示例已修改为 <code>tc-example.c</code> 示例，可以使用 tc 加载并附加到网络设备的入口和出口挂钩。它将传输的字节记入一个名为 <code>acc_map</code> 的映射，该映射有两个映射槽，一个用于在入口钩子上计算流量，一个用于在出口钩子上计算流量。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#include &lt;linux/bpf.h&gt;</span>
<span class="token comment">#include &lt;linux/pkt_cls.h&gt;</span>
<span class="token comment">#include &lt;stdint.h&gt;</span>
<span class="token comment">#include &lt;iproute2/bpf_elf.h&gt;</span>

<span class="token comment">#ifndef __section</span>
<span class="token comment"># define __section(NAME)                  \\</span>
   __attribute__<span class="token variable"><span class="token punctuation">((</span>section<span class="token punctuation">(</span>NAME<span class="token punctuation">)</span><span class="token punctuation">,</span> used<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef __inline</span>
<span class="token comment"># define __inline                         \\</span>
   inline __attribute__<span class="token variable"><span class="token punctuation">((</span>always_inline<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef lock_xadd</span>
<span class="token comment"># define lock_xadd(ptr, val)              \\</span>
   <span class="token variable"><span class="token punctuation">((</span>void<span class="token punctuation">)</span>__sync_fetch_and_add<span class="token punctuation">(</span>ptr<span class="token punctuation">,</span> val<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef BPF_FUNC</span>
<span class="token comment"># define BPF_FUNC(NAME, ...)              \\</span>
   <span class="token punctuation">(</span>*NAME<span class="token punctuation">)</span><span class="token punctuation">(</span>__VA_ARGS__<span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token punctuation">(</span>void *<span class="token punctuation">)</span>BPF_FUNC_<span class="token comment">##NAME</span>
<span class="token comment">#endif</span>

static void *BPF_FUNC<span class="token punctuation">(</span>map_lookup_elem, void *map, const void *key<span class="token punctuation">)</span><span class="token punctuation">;</span>

struct bpf_elf_map acc_map __section<span class="token punctuation">(</span><span class="token string">&quot;maps&quot;</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token punctuation">{</span>
    .type           <span class="token operator">=</span> BPF_MAP_TYPE_ARRAY,
    .size_key       <span class="token operator">=</span> sizeof<span class="token punctuation">(</span>uint32_t<span class="token punctuation">)</span>,
    .size_value     <span class="token operator">=</span> sizeof<span class="token punctuation">(</span>uint32_t<span class="token punctuation">)</span>,
    .pinning        <span class="token operator">=</span> PIN_GLOBAL_NS,
    .max_elem       <span class="token operator">=</span> <span class="token number">2</span>,
<span class="token punctuation">}</span><span class="token punctuation">;</span>

static __inline int account_data<span class="token punctuation">(</span>struct __sk_buff *skb, uint32_t <span class="token function">dir</span><span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    uint32_t *bytes<span class="token punctuation">;</span>

    bytes <span class="token operator">=</span> map_lookup_elem<span class="token punctuation">(</span><span class="token operator">&amp;</span>acc_map, <span class="token operator">&amp;</span><span class="token function">dir</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>bytes<span class="token punctuation">)</span>
            lock_xadd<span class="token punctuation">(</span>bytes, skb-<span class="token operator">&gt;</span>len<span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token builtin class-name">return</span> TC_ACT_OK<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

__section<span class="token punctuation">(</span><span class="token string">&quot;ingress&quot;</span><span class="token punctuation">)</span>
int tc_ingress<span class="token punctuation">(</span>struct __sk_buff *skb<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    <span class="token builtin class-name">return</span> account_data<span class="token punctuation">(</span>skb, <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

__section<span class="token punctuation">(</span><span class="token string">&quot;egress&quot;</span><span class="token punctuation">)</span>
int tc_egress<span class="token punctuation">(</span>struct __sk_buff *skb<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    <span class="token builtin class-name">return</span> account_data<span class="token punctuation">(</span>skb, <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

char __license<span class="token punctuation">[</span><span class="token punctuation">]</span> __section<span class="token punctuation">(</span><span class="token string">&quot;license&quot;</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token string">&quot;GPL&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>该示例还演示了开发程序时需要注意的其他一些事项。该代码包括内核头文件、标准 C 头文件和包含 <code>struct bpf_elf_map</code> 定义的 iproute2 特定头文件。 iproute2 有一个通用的 BPF ELF 加载器，因此 <code>struct bpf_elf_map</code> 的定义对于 XDP 和 tc 类型程序来说是相同的。</p><p><code>struct bpf_elf_map</code> 条目定义了程序中的映射，并包含生成两个 BPF 程序使用的映射所需的所有相关信息（例如键/值大小等）。该结构必须放入 <code>maps</code> 部分，以便加载器可以找到它。可以有多个具有不同变量名称的此类映射声明，但所有声明都必须使用 <code>__section(&quot;maps&quot;)</code> 进行注释。</p><p><code>struct bpf_elf_map</code> 特定于 iproute2。不同的 BPF ELF 加载器可以有不同的格式，例如内核源码树中主要由 <code>perf</code> 使用的 libbpf 就有不同的规范。 iproute2 保证 <code>struct bpf_elf_map</code> 的向后兼容性。 Cilium 遵循 iproute2 模型。</p><p>该示例还演示了如何将 BPF 辅助函数映射到 C 代码并使用。这里， <code>map_lookup_elem()</code> 是通过将此函数映射到 <code>BPF_FUNC_map_lookup_elem</code> 枚举值来定义的，该枚举值在 <code>uapi/linux/bpf.h</code> 中作为助手公开。当程序稍后加载到内核中时，验证程序会检查传递的参数是否属于预期类型，并将帮助程序调用重新指向真正的函数调用。此外， <code>map_lookup_elem()</code> 还演示了如何将映射传递给 BPF 辅助函数。这里， <code>maps</code> 部分中的 <code>&amp;acc_map</code> 作为第一个参数传递给 <code>map_lookup_elem()</code> 。</p><p>由于定义的数组映射是全局的，因此记账需要使用原子操作，定义为 <code>lock_xadd()</code> 。 LLVM 将 <code>__sync_fetch_and_add()</code> 作为内置函数映射到 BPF 原子添加指令，即表示字大小的 <code>BPF_STX | BPF_XADD | BPF_W</code> 。</p><p>最后但并非最不重要的一点是， <code>struct bpf_elf_map</code> 告诉地图将被固定为 <code>PIN_GLOBAL_NS</code> 。这意味着 tc 会将映射作为节点固定到 BPF 伪文件系统中。默认情况下，对于给定的示例，它将固定到 <code>/sys/fs/bpf/tc/globals/acc_map</code> 。由于 <code>PIN_GLOBAL_NS</code> ，地图将放置在 <code>/sys/fs/bpf/tc/globals/</code> 下。 <code>globals</code> 充当跨对象文件的全局命名空间。如果示例使用 <code>PIN_OBJECT_NS</code> ，那么 tc 将创建一个目标文件本地目录。例如，具有 BPF 代码的不同 C 文件可以具有与上面相同的 <code>acc_map</code> 定义，并带有 <code>PIN_GLOBAL_NS</code> 固定。在这种情况下，映射将在源自各种目标文件的 BPF 程序之间共享。 <code>PIN_NONE</code> 意味着该映射不会作为节点放入 BPF 文件系统中，因此在 tc 退出后将无法从用户空间访问。这也意味着 tc 为每个程序创建两个单独的地图实例，因为它无法检索以前固定在该名称下的地图。上述路径中的 <code>acc_map</code> 部分是源代码中指定的地图名称。</p><p>因此，在加载 <code>ingress</code> 程序时，tc会发现BPF文件系统中不存在这样的映射，并创建一个新的映射。成功后，该映射也将被固定，这样当通过 tc 加载 <code>egress</code> 程序时，它会发现该映射已存在于 BPF 文件系统中，并将其重用于 <code>egress</code></p><p>就像 tc 可以检索相同的映射一样，第三方应用程序也可以使用 bpf 系统调用中的 <code>BPF_OBJ_GET</code> 命令来创建指向相同映射实例的新文件描述符，然后可以将其用于查找/更新/删除地图元素。</p><p>代码可以通过 iproute2 编译并加载，如下所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ clang <span class="token parameter variable">-O2</span> <span class="token parameter variable">-Wall</span> <span class="token parameter variable">--target</span><span class="token operator">=</span>bpf <span class="token parameter variable">-c</span> tc-example.c <span class="token parameter variable">-o</span> tc-example.o

<span class="token comment"># tc qdisc add dev em1 clsact</span>
<span class="token comment"># tc filter add dev em1 ingress bpf da obj tc-example.o sec ingress</span>
<span class="token comment"># tc filter add dev em1 egress bpf da obj tc-example.o sec egress</span>

<span class="token comment"># tc filter show dev em1 ingress</span>
filter protocol all pref <span class="token number">49152</span> bpf
filter protocol all pref <span class="token number">49152</span> bpf handle 0x1 tc-example.o:<span class="token punctuation">[</span>ingress<span class="token punctuation">]</span> direct-action <span class="token function">id</span> <span class="token number">1</span> tag c5f7825e5dac396f

<span class="token comment"># tc filter show dev em1 egress</span>
filter protocol all pref <span class="token number">49152</span> bpf
filter protocol all pref <span class="token number">49152</span> bpf handle 0x1 tc-example.o:<span class="token punctuation">[</span>egress<span class="token punctuation">]</span> direct-action <span class="token function">id</span> <span class="token number">2</span> tag b2fd5adc0f262714

<span class="token comment"># mount | grep bpf</span>
sysfs on /sys/fs/bpf <span class="token builtin class-name">type</span> sysfs <span class="token punctuation">(</span>rw,nosuid,nodev,noexec,relatime,seclabel<span class="token punctuation">)</span>
bpf on /sys/fs/bpf <span class="token builtin class-name">type</span> bpf <span class="token punctuation">(</span>rw,relatime,mode<span class="token operator">=</span>0700<span class="token punctuation">)</span>

<span class="token comment"># tree /sys/fs/bpf/</span>
/sys/fs/bpf/
+-- <span class="token function">ip</span> -<span class="token operator">&gt;</span> /sys/fs/bpf/tc/
+-- tc
<span class="token operator">|</span>   +-- globals
<span class="token operator">|</span>       +-- acc_map
+-- xdp -<span class="token operator">&gt;</span> /sys/fs/bpf/tc/

<span class="token number">4</span> directories, <span class="token number">1</span> <span class="token function">file</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>一旦数据包通过 <code>em1</code> 设备，BPF 映射中的计数器就会增加。</p><h4 id="不允许使用全局变量" tabindex="-1"><a class="header-anchor" href="#不允许使用全局变量"><span>不允许使用全局变量</span></a></h4><p>由于第 1 点中已经提到的原因，BPF 不能像普通 C 程序中那样具有全局变量。</p><p>然而，有一个解决方法，程序可以简单地使用 <code>BPF_MAP_TYPE_PERCPU_ARRAY</code> 类型的 BPF 映射，其中只有一个任意值大小的槽。这是可行的，因为在执行期间，BPF 程序保证永远不会被内核抢占，因此可以使用单个映射条目作为临时数据的暂存缓冲区，例如，以超出堆栈限制。这也适用于尾部调用，因为它在抢占方面具有相同的保证。</p><p>否则，为了在多个 BPF 程序运行之间保持状态，可以使用普通的 BPF 映射。</p><h4 id="不允许使用-const-字符串或数组" tabindex="-1"><a class="header-anchor" href="#不允许使用-const-字符串或数组"><span>不允许使用 const 字符串或数组</span></a></h4><p>在 BPF C 程序中定义 <code>const</code> 字符串或其他数组不起作用，原因与第 1 节和第 3 节中指出的相同，即，将在 ELF 文件中生成重定位条目，该重定位条目将被拒绝由于不是加载器 ABI 的一部分（加载器也无法修复此类条目，因为它需要对已编译的 BPF 序列进行大量重写）。</p><p>将来，LLVM 可能会检测到这些情况并尽早向用户抛出错误。</p><p>诸如 <code>trace_printk()</code> 之类的辅助函数可以按如下方式解决：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>static void BPF_FUNC<span class="token punctuation">(</span>trace_printk, const char *fmt, int fmt_size, <span class="token punctuation">..</span>.<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">#ifndef printk</span>
<span class="token comment"># define printk(fmt, ...)                                      \\</span>
    <span class="token punctuation">(</span><span class="token punctuation">{</span>                                                         <span class="token punctuation">\\</span>
        char ____fmt<span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token function">fmt</span><span class="token punctuation">;</span>                                  <span class="token punctuation">\\</span>
        trace_printk<span class="token punctuation">(</span>____fmt, sizeof<span class="token punctuation">(</span>____fmt<span class="token punctuation">)</span>, <span class="token comment">##__VA_ARGS__); \\</span>
    <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">#endif</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>然后程序可以像 <code>printk(&quot;skb len:%u\\n&quot;, skb-&gt;len);</code> 一样自然地使用宏。然后输出将被写入跟踪管道。 <code>tc exec bpf dbg</code> 可用于从那里检索消息。</p><p>使用 <code>trace_printk()</code> 辅助函数有一些缺点，因此不建议在生产中使用。每次调用辅助函数时，像 <code>&quot;skb len:%u\\n&quot;</code> 这样的常量字符串都需要加载到 BPF 堆栈中，而且 BPF 辅助函数最多只能有 5 个参数。这仅留下了 3 个可用于转储的附加变量的空间。</p><p>因此，尽管有助于快速调试，但建议（对于网络程序）分别使用 <code>skb_event_output()</code> 或 <code>xdp_event_output()</code> 帮助器。它们允许将自定义结构从 BPF 程序传递到 perf 事件环缓冲区以及可选的数据包样本。例如，Cilium 的监视器利用这些帮助程序来实现调试框架、网络策略违规通知等。这些帮助程序通过映射每个 CPU <code>perf</code> 环形缓冲区的无锁内存传递数据，并且因此比 <code>trace_printk()</code> 快得多。</p><h4 id="使用-llvm-内置函数-memset-memcpy-memmove-memcmp" tabindex="-1"><a class="header-anchor" href="#使用-llvm-内置函数-memset-memcpy-memmove-memcmp"><span>使用 LLVM 内置函数 memset()/memcpy()/memmove()/memcmp()</span></a></h4><p>由于 BPF 程序无法执行除 BPF 助手之外的任何函数调用，因此公共库代码需要实现为内联函数。此外，LLVM 还提供了一些内置函数，程序可以将其用于恒定大小（此处： <code>n</code> ），然后它们将始终被内联：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#ifndef memset</span>
<span class="token comment"># define memset(dest, chr, n)   __builtin_memset((dest), (chr), (n))</span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef memcpy</span>
<span class="token comment"># define memcpy(dest, src, n)   __builtin_memcpy((dest), (src), (n))</span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef memmove</span>
<span class="token comment"># define memmove(dest, src, n)  __builtin_memmove((dest), (src), (n))</span>
<span class="token comment">#endif</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>memcmp()</code> 内置函数存在一些极端情况，由于后端的 LLVM 问题，内联没有发生，因此在问题解决之前不建议使用。</p><h4 id="目前还没有可用的循环" tabindex="-1"><a class="header-anchor" href="#目前还没有可用的循环"><span>目前还没有可用的循环</span></a></h4><p>除了其他控制流图验证之外，内核中的 BPF 验证器还通过对所有可能的程序路径执行深度优先搜索来检查 BPF 程序是否不包含循环。目的是确保程序始终保证终止。</p><p>通过使用 <code>#pragma unroll</code> 指令，可以使用非常有限的循环形式来实现恒定的循环上限。编译为 BPF 的示例代码：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#pragma unroll</span>
    <span class="token keyword">for</span> <span class="token punctuation">(</span>i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> IPV6_MAX_HEADERS<span class="token punctuation">;</span> i++<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        switch <span class="token punctuation">(</span>nh<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token keyword">case</span> NEXTHDR_NONE:
            <span class="token builtin class-name">return</span> DROP_INVALID_EXTHDR<span class="token punctuation">;</span>
        <span class="token keyword">case</span> NEXTHDR_FRAGMENT:
            <span class="token builtin class-name">return</span> DROP_FRAG_NOSUPPORT<span class="token punctuation">;</span>
        <span class="token keyword">case</span> NEXTHDR_HOP:
        <span class="token keyword">case</span> NEXTHDR_ROUTING:
        <span class="token keyword">case</span> NEXTHDR_AUTH:
        <span class="token keyword">case</span> NEXTHDR_DEST:
            <span class="token keyword">if</span> <span class="token punctuation">(</span>skb_load_bytes<span class="token punctuation">(</span>skb, l3_off + len, <span class="token operator">&amp;</span>opthdr, sizeof<span class="token punctuation">(</span>opthdr<span class="token punctuation">))</span> <span class="token operator">&lt;</span> <span class="token number">0</span><span class="token punctuation">)</span>
                <span class="token builtin class-name">return</span> DROP_INVALID<span class="token punctuation">;</span>

            nh <span class="token operator">=</span> opthdr.nexthdr<span class="token punctuation">;</span>
            <span class="token keyword">if</span> <span class="token punctuation">(</span>nh <span class="token operator">==</span> NEXTHDR_AUTH<span class="token punctuation">)</span>
                len <span class="token operator">+=</span> ipv6_authlen<span class="token punctuation">(</span><span class="token operator">&amp;</span>opthdr<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token keyword">else</span>
                len <span class="token operator">+=</span> ipv6_optlen<span class="token punctuation">(</span><span class="token operator">&amp;</span>opthdr<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token builtin class-name">break</span><span class="token punctuation">;</span>
        default:
            *nexthdr <span class="token operator">=</span> nh<span class="token punctuation">;</span>
            <span class="token builtin class-name">return</span> len<span class="token punctuation">;</span>
        <span class="token punctuation">}</span>
    <span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>另一种可能性是通过再次调用同一程序并使用 <code>BPF_MAP_TYPE_PERCPU_ARRAY</code> 映射来获得本地暂存空间，从而使用尾部调用。虽然是动态的，但这种形式的循环仅限于最多 34 次迭代（初始程序，加上尾部调用的 33 次迭代）。</p><p>将来，BPF 可能会有一些本机但有限形式的实现循环。</p><h4 id="使用尾调用对程序进行分区" tabindex="-1"><a class="header-anchor" href="#使用尾调用对程序进行分区"><span>使用尾调用对程序进行分区</span></a></h4><p>尾部调用提供了在运行时通过从一个 BPF 程序跳转到另一个 BPF 程序以原子方式改变程序行为的灵活性。为了选择下一个程序，尾部调用使用程序数组映射（ <code>BPF_MAP_TYPE_PROG_ARRAY</code> ），并将映射以及索引传递给要跳转到的下一个程序。执行跳转后不会返回到旧程序，并且如果给定映射索引处不存在程序，则继续执行原始程序。</p><p>例如，这可以用于实现解析器的各个阶段，其中可以在运行时使用新的解析功能来更新这些阶段。</p><p>另一个用例是事件通知，例如，Cilium 可以在运行时选择数据包丢失通知，其中 <code>skb_event_output()</code> 调用位于尾部调用的程序内。因此，在正常操作期间，除非将程序添加到相关映射索引，否则将始终执行失败路径，然后该程序准备元数据并向用户空间守护程序触发事件通知。</p><p>程序数组映射非常灵活，还可以对位于每个映射索引中的程序执行单独的操作。例如，附加到 XDP 或 tc 的根程序可以对程序数组映射的索引 0 执行初始尾部调用，执行流量采样，然后跳转到程序数组映射的索引 1，其中应用防火墙策略并且数据包在程序数组映射的索引 2 中丢弃或进一步处理，在那里它被破坏并再次从接口发送出去。当然，程序数组映射中的跳转可以是任意的。当达到最大尾调用限制时，内核最终将执行失败路径。</p><p>使用尾部调用的最小示例摘录：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>

<span class="token comment">#ifndef __stringify</span>
<span class="token comment"># define __stringify(X)   #X</span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef __section</span>
<span class="token comment"># define __section(NAME)                  \\</span>
   __attribute__<span class="token variable"><span class="token punctuation">((</span>section<span class="token punctuation">(</span>NAME<span class="token punctuation">)</span><span class="token punctuation">,</span> used<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef __section_tail</span>
<span class="token comment"># define __section_tail(ID, KEY)          \\</span>
   __section<span class="token punctuation">(</span>__stringify<span class="token punctuation">(</span>ID<span class="token punctuation">)</span> <span class="token string">&quot;/&quot;</span> __stringify<span class="token punctuation">(</span>KEY<span class="token punctuation">))</span>
<span class="token comment">#endif</span>

<span class="token comment">#ifndef BPF_FUNC</span>
<span class="token comment"># define BPF_FUNC(NAME, ...)              \\</span>
   <span class="token punctuation">(</span>*NAME<span class="token punctuation">)</span><span class="token punctuation">(</span>__VA_ARGS__<span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token punctuation">(</span>void *<span class="token punctuation">)</span>BPF_FUNC_<span class="token comment">##NAME</span>
<span class="token comment">#endif</span>

<span class="token comment">#define BPF_JMP_MAP_ID   1</span>

static void BPF_FUNC<span class="token punctuation">(</span>tail_call, struct __sk_buff *skb, void *map,
                     uint32_t index<span class="token punctuation">)</span><span class="token punctuation">;</span>

struct bpf_elf_map jmp_map __section<span class="token punctuation">(</span><span class="token string">&quot;maps&quot;</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token punctuation">{</span>
    .type           <span class="token operator">=</span> BPF_MAP_TYPE_PROG_ARRAY,
    .id             <span class="token operator">=</span> BPF_JMP_MAP_ID,
    .size_key       <span class="token operator">=</span> sizeof<span class="token punctuation">(</span>uint32_t<span class="token punctuation">)</span>,
    .size_value     <span class="token operator">=</span> sizeof<span class="token punctuation">(</span>uint32_t<span class="token punctuation">)</span>,
    .pinning        <span class="token operator">=</span> PIN_GLOBAL_NS,
    .max_elem       <span class="token operator">=</span> <span class="token number">1</span>,
<span class="token punctuation">}</span><span class="token punctuation">;</span>

__section_tail<span class="token punctuation">(</span>BPF_JMP_MAP_ID, <span class="token number">0</span><span class="token punctuation">)</span>
int looper<span class="token punctuation">(</span>struct __sk_buff *skb<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    printk<span class="token punctuation">(</span><span class="token string">&quot;skb cb: %u<span class="token entity" title="\\n">\\n</span>&quot;</span>, skb-<span class="token operator">&gt;</span>cb<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span>++<span class="token punctuation">)</span><span class="token punctuation">;</span>
    tail_call<span class="token punctuation">(</span>skb, <span class="token operator">&amp;</span>jmp_map, <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token builtin class-name">return</span> TC_ACT_OK<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

__section<span class="token punctuation">(</span><span class="token string">&quot;prog&quot;</span><span class="token punctuation">)</span>
int entry<span class="token punctuation">(</span>struct __sk_buff *skb<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    skb-<span class="token operator">&gt;</span>cb<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
    tail_call<span class="token punctuation">(</span>skb, <span class="token operator">&amp;</span>jmp_map, <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token builtin class-name">return</span> TC_ACT_OK<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

char __license<span class="token punctuation">[</span><span class="token punctuation">]</span> __section<span class="token punctuation">(</span><span class="token string">&quot;license&quot;</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token string">&quot;GPL&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>当加载这个玩具程序时，tc 将创建程序数组并将其固定到 <code>jmp_map</code> 下全局命名空间中的 BPF 文件系统。此外，iproute2 中的 BPF ELF 加载器也会识别标记为 <code>__section_tail()</code> 的部分。 <code>struct bpf_elf_map</code> 中提供的 <code>id</code> 将与 <code>__section_tail()</code> 中的 id 标记进行匹配，即 <code>JMP_MAP_ID</code> ，因此加载程序在用户指定的程序数组映射索引处，在此示例中为 <code>0</code> 。因此，所有提供的尾部调用部分将由 iproute2 加载器填充到相应的映射中。该机制并非特定于 tc，而是可以应用于 iproute2 支持的任何其他 BPF 程序类型（例如 XDP、lwt）。</p><p>生成的 elf 包含描述地图 id 和该地图中的条目的节标题：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ llvm-objdump <span class="token parameter variable">-S</span> --no-show-raw-insn prog_array.o <span class="token operator">|</span> <span class="token function">less</span>
prog_array.o:   <span class="token function">file</span> <span class="token function">format</span> ELF64-BPF

Disassembly of section <span class="token number">1</span>/0:
looper:
       <span class="token number">0</span>:       r6 <span class="token operator">=</span> r1
       <span class="token number">1</span>:       r2 <span class="token operator">=</span> *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r6 + <span class="token number">48</span><span class="token punctuation">)</span>
       <span class="token number">2</span>:       r1 <span class="token operator">=</span> r2
       <span class="token number">3</span>:       r1 <span class="token operator">+=</span> <span class="token number">1</span>
       <span class="token number">4</span>:       *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r6 + <span class="token number">48</span><span class="token punctuation">)</span> <span class="token operator">=</span> r1
       <span class="token number">5</span>:       r1 <span class="token operator">=</span> <span class="token number">0</span> ll
       <span class="token number">7</span>:       call <span class="token parameter variable">-1</span>
       <span class="token number">8</span>:       r1 <span class="token operator">=</span> r6
       <span class="token number">9</span>:       r2 <span class="token operator">=</span> <span class="token number">0</span> ll
      <span class="token number">11</span>:       r3 <span class="token operator">=</span> <span class="token number">0</span>
      <span class="token number">12</span>:       call <span class="token number">12</span>
      <span class="token number">13</span>:       r0 <span class="token operator">=</span> <span class="token number">0</span>
      <span class="token number">14</span>:       <span class="token builtin class-name">exit</span>
Disassembly of section prog:
entry:
       <span class="token number">0</span>:       r2 <span class="token operator">=</span> <span class="token number">0</span>
       <span class="token number">1</span>:       *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r1 + <span class="token number">48</span><span class="token punctuation">)</span> <span class="token operator">=</span> r2
       <span class="token number">2</span>:       r2 <span class="token operator">=</span> <span class="token number">0</span> ll
       <span class="token number">4</span>:       r3 <span class="token operator">=</span> <span class="token number">0</span>
       <span class="token number">5</span>:       call <span class="token number">12</span>
       <span class="token number">6</span>:       r0 <span class="token operator">=</span> <span class="token number">0</span>
       <span class="token number">7</span>:       exi
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在本例中， <code>section 1/0</code> 指示 <code>looper()</code> 函数驻留在地图 id <code>1</code> 的位置 <code>0</code> 处。</p><p>固定的地图可以由用户空间应用程序（例如 Cilium 守护程序）检索，也可以由 tc 本身检索，以便使用新程序更新地图。更新以原子方式发生，首先从各个子系统触发的初始入口程序也以原子方式更新。</p><p>tc 执行尾调用映射更新的示例：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ tc <span class="token builtin class-name">exec</span> bpf graft m:globals/jmp_map key <span class="token number">0</span> obj new.o sec foo
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果 iproute2 将更新固定程序数组，则可以使用 <code>graft</code> 命令。通过将其指向 <code>globals/jmp_map</code> ，tc 将使用位于 <code>foo</code> 中的新程序来更新索引/键 <code>0</code> 处的映射。 /b4&gt; .</p><h4 id="堆栈空间有限-最多-512-字节。" tabindex="-1"><a class="header-anchor" href="#堆栈空间有限-最多-512-字节。"><span>堆栈空间有限，最多 512 字节。</span></a></h4><p>BPF 程序中的堆栈空间仅限于 512 字节，在用 C 语言实现 BPF 程序时需要仔细考虑这一点。但是，如前面第 3 点所述，具有单个条目的 <code>BPF_MAP_TYPE_PERCPU_ARRAY</code> 映射可以用于扩大暂存缓冲区空间。</p><h4 id="可以使用-bpf-内联组装" tabindex="-1"><a class="header-anchor" href="#可以使用-bpf-内联组装"><span>可以使用 BPF 内联组装</span></a></h4><p>LLVM 6.0 或更高版本允许在可能需要的极少数情况下使用 BPF 内联汇编。下面的（废话）玩具示例显示了 64 位原子添加。由于缺乏文档， <code>lib/Target/BPF/BPFInstrInfo.td</code> 和 <code>test/CodeGen/BPF/</code> 中的 LLVM 源代码可能有助于提供一些其他示例。测试代码：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#include &lt;linux/bpf.h&gt;</span>

<span class="token comment">#ifndef __section</span>
<span class="token comment"># define __section(NAME)                  \\</span>
   __attribute__<span class="token variable"><span class="token punctuation">((</span>section<span class="token punctuation">(</span>NAME<span class="token punctuation">)</span><span class="token punctuation">,</span> used<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

__section<span class="token punctuation">(</span><span class="token string">&quot;prog&quot;</span><span class="token punctuation">)</span>
int xdp_test<span class="token punctuation">(</span>struct xdp_md *ctx<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    __u64 a <span class="token operator">=</span> <span class="token number">2</span>, b <span class="token operator">=</span> <span class="token number">3</span>, *c <span class="token operator">=</span> <span class="token operator">&amp;</span>a<span class="token punctuation">;</span>
    /* just a toy xadd example to show the syntax */
    asm volatile<span class="token punctuation">(</span><span class="token string">&quot;lock *(u64 *)(%0+0) += %1&quot;</span> <span class="token builtin class-name">:</span> <span class="token string">&quot;=r&quot;</span><span class="token punctuation">(</span>c<span class="token punctuation">)</span> <span class="token builtin class-name">:</span> <span class="token string">&quot;r&quot;</span><span class="token punctuation">(</span>b<span class="token punctuation">)</span>, <span class="token string">&quot;0&quot;</span><span class="token punctuation">(</span>c<span class="token punctuation">))</span><span class="token punctuation">;</span>
    <span class="token builtin class-name">return</span> a<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

char __license<span class="token punctuation">[</span><span class="token punctuation">]</span> __section<span class="token punctuation">(</span><span class="token string">&quot;license&quot;</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token string">&quot;GPL&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>上述程序被编译成以下BPF指令序列：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>Verifier analysis:

<span class="token number">0</span>: <span class="token punctuation">(</span>b7<span class="token punctuation">)</span> r1 <span class="token operator">=</span> <span class="token number">2</span>
<span class="token number">1</span>: <span class="token punctuation">(</span>7b<span class="token punctuation">)</span> *<span class="token punctuation">(</span>u64 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r10 -8<span class="token punctuation">)</span> <span class="token operator">=</span> r1
<span class="token number">2</span>: <span class="token punctuation">(</span>b7<span class="token punctuation">)</span> r1 <span class="token operator">=</span> <span class="token number">3</span>
<span class="token number">3</span>: <span class="token punctuation">(</span>bf<span class="token punctuation">)</span> r2 <span class="token operator">=</span> r10
<span class="token number">4</span>: <span class="token punctuation">(</span>07<span class="token punctuation">)</span> r2 <span class="token operator">+=</span> <span class="token parameter variable">-8</span>
<span class="token number">5</span>: <span class="token punctuation">(</span>db<span class="token punctuation">)</span> lock *<span class="token punctuation">(</span>u64 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r2 +0<span class="token punctuation">)</span> <span class="token operator">+=</span> r1
<span class="token number">6</span>: <span class="token punctuation">(</span><span class="token number">79</span><span class="token punctuation">)</span> r0 <span class="token operator">=</span> *<span class="token punctuation">(</span>u64 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r10 -8<span class="token punctuation">)</span>
<span class="token number">7</span>: <span class="token punctuation">(</span><span class="token number">95</span><span class="token punctuation">)</span> <span class="token builtin class-name">exit</span>
processed <span class="token number">8</span> insns <span class="token punctuation">(</span>limit <span class="token number">131072</span><span class="token punctuation">)</span>, stack depth <span class="token number">8</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="使用-pragma-pack-删除结构填充并对齐成员" tabindex="-1"><a class="header-anchor" href="#使用-pragma-pack-删除结构填充并对齐成员"><span>使用 #pragma pack 删除结构填充并对齐成员</span></a></h4><p>在现代编译器中，数据结构默认对齐以有效地访问内存。结构成员被打包到内存地址，并添加填充以与处理器字大小正确对齐（例如，64 位处理器为 8 字节，32 位处理器为 4 字节）。因此，结构的大小通常可能会比预期的大。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>struct called_info <span class="token punctuation">{</span>
    u64 start<span class="token punctuation">;</span>  // <span class="token number">8</span>-byte
    u64 end<span class="token punctuation">;</span>    // <span class="token number">8</span>-byte
    u32 sector<span class="token punctuation">;</span> // <span class="token number">4</span>-byte
<span class="token punctuation">}</span><span class="token punctuation">;</span> // size of <span class="token number">20</span>-byte ?

printf<span class="token punctuation">(</span><span class="token string">&quot;size of %d-byte<span class="token entity" title="\\n">\\n</span>&quot;</span>, sizeof<span class="token punctuation">(</span>struct called_info<span class="token punctuation">))</span><span class="token punctuation">;</span> // size of <span class="token number">24</span>-byte

// Actual compiled composition of struct called_info
// 0x0<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span>                   0x8<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>
//  ↓________________________↓
//  <span class="token operator">|</span>        start <span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>       <span class="token operator">|</span>
//  <span class="token operator">|</span>________________________<span class="token operator">|</span>
//  <span class="token operator">|</span>         end  <span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>       <span class="token operator">|</span>
//  <span class="token operator">|</span>________________________<span class="token operator">|</span>
//  <span class="token operator">|</span>  sector<span class="token punctuation">(</span><span class="token number">4</span><span class="token punctuation">)</span> <span class="token operator">|</span>  PADDING  <span class="token operator">|</span> <span class="token operator">&lt;=</span> address aligned to <span class="token number">8</span>
//  <span class="token operator">|</span>____________<span class="token operator">|</span>___________<span class="token operator">|</span>     with <span class="token number">4</span>-byte PADDING.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>内核中的 BPF 验证器检查 BPF 程序不会访问边界外或未初始化堆栈区域的堆栈边界。使用带有 padding 的 struct 作为映射值，将导致 <code>invalid indirect read from stack</code> 在 <code>bpf_prog_load()</code> 上失败。</p><p>示例代码：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>struct called_info <span class="token punctuation">{</span>
    u64 start<span class="token punctuation">;</span>
    u64 end<span class="token punctuation">;</span>
    u32 sector<span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

struct bpf_map_def SEC<span class="token punctuation">(</span><span class="token string">&quot;maps&quot;</span><span class="token punctuation">)</span> called_info_map <span class="token operator">=</span> <span class="token punctuation">{</span>
    .type <span class="token operator">=</span> BPF_MAP_TYPE_HASH,
    .key_size <span class="token operator">=</span> sizeof<span class="token punctuation">(</span>long<span class="token punctuation">)</span>,
    .value_size <span class="token operator">=</span> sizeof<span class="token punctuation">(</span>struct called_info<span class="token punctuation">)</span>,
    .max_entries <span class="token operator">=</span> <span class="token number">4096</span>,
<span class="token punctuation">}</span><span class="token punctuation">;</span>

SEC<span class="token punctuation">(</span><span class="token string">&quot;kprobe/submit_bio&quot;</span><span class="token punctuation">)</span>
int submit_bio_entry<span class="token punctuation">(</span>struct pt_regs *ctx<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    char fmt<span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token string">&quot;submit_bio(bio=0x%lx) called: %llu<span class="token entity" title="\\n">\\n</span>&quot;</span><span class="token punctuation">;</span>
    u64 start_time <span class="token operator">=</span> bpf_ktime_get_ns<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    long bio_ptr <span class="token operator">=</span> PT_REGS_PARM1<span class="token punctuation">(</span>ctx<span class="token punctuation">)</span><span class="token punctuation">;</span>
    struct called_info called_info <span class="token operator">=</span> <span class="token punctuation">{</span>
            .start <span class="token operator">=</span> start_time,
            .end <span class="token operator">=</span> <span class="token number">0</span>,
            .sector <span class="token operator">=</span> <span class="token number">0</span>
    <span class="token punctuation">}</span><span class="token punctuation">;</span>

    bpf_map_update_elem<span class="token punctuation">(</span><span class="token operator">&amp;</span>called_info_map, <span class="token operator">&amp;</span>bio_ptr, <span class="token operator">&amp;</span>called_info, BPF_ANY<span class="token punctuation">)</span><span class="token punctuation">;</span>
    bpf_trace_printk<span class="token punctuation">(</span>fmt, sizeof<span class="token punctuation">(</span>fmt<span class="token punctuation">)</span>, bio_ptr, start_time<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token builtin class-name">return</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>bpf_load_program()</code> 上的相应输出：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>bpf_load_program<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token assign-left variable">err</span><span class="token operator">=</span><span class="token number">13</span>
<span class="token number">0</span>: <span class="token punctuation">(</span>bf<span class="token punctuation">)</span> r6 <span class="token operator">=</span> r1
<span class="token punctuation">..</span>.
<span class="token number">19</span>: <span class="token punctuation">(</span>b7<span class="token punctuation">)</span> r1 <span class="token operator">=</span> <span class="token number">0</span>
<span class="token number">20</span>: <span class="token punctuation">(</span>7b<span class="token punctuation">)</span> *<span class="token punctuation">(</span>u64 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r10 -72<span class="token punctuation">)</span> <span class="token operator">=</span> r1
<span class="token number">21</span>: <span class="token punctuation">(</span>7b<span class="token punctuation">)</span> *<span class="token punctuation">(</span>u64 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r10 -80<span class="token punctuation">)</span> <span class="token operator">=</span> r7
<span class="token number">22</span>: <span class="token punctuation">(</span><span class="token number">63</span><span class="token punctuation">)</span> *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r10 -64<span class="token punctuation">)</span> <span class="token operator">=</span> r1
<span class="token punctuation">..</span>.
<span class="token number">30</span>: <span class="token punctuation">(</span><span class="token number">85</span><span class="token punctuation">)</span> call bpf_map_update_elem<span class="token comment">#2</span>
invalid indirect <span class="token builtin class-name">read</span> from stack off -80+20 size <span class="token number">24</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在 <code>bpf_prog_load()</code> 处，调用 eBPF 验证器 <code>bpf_check()</code> ，它将通过调用 <code>check_func_arg() -&gt; check_stack_boundary()</code> 检查堆栈边界。从上面的错误显示， <code>struct called_info</code> 被编译为24字节大小，并且消息说从+20读取数据是无效的间接读取。正如我们之前讨论的，地址 0x14(20) 是 PADDING 所在的位置。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>// Actual compiled composition of struct called_info
// 0x10<span class="token punctuation">(</span><span class="token number">16</span><span class="token punctuation">)</span>    0x14<span class="token punctuation">(</span><span class="token number">20</span><span class="token punctuation">)</span>    0x18<span class="token punctuation">(</span><span class="token number">24</span><span class="token punctuation">)</span>
//  ↓____________↓___________↓
//  <span class="token operator">|</span>  sector<span class="token punctuation">(</span><span class="token number">4</span><span class="token punctuation">)</span> <span class="token operator">|</span>  PADDING  <span class="token operator">|</span> <span class="token operator">&lt;=</span> address aligned to <span class="token number">8</span>
//  <span class="token operator">|</span>____________<span class="token operator">|</span>___________<span class="token operator">|</span>     with <span class="token number">4</span>-byte PADDING.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>check_stack_boundary()</code> 在内部循环遍历从起始指针开始的每个 <code>access_size</code> (24) 字节，以确保它位于堆栈边界内并且堆栈的所有元素都已初始化。由于不应该使用填充，因此它会出现“从堆栈中间接读取无效”的错误。为了避免这种失败，需要从结构中删除填充。</p><p>使用 <code>#pragma pack(n)</code> 指令删除填充：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#pragma pack(4)</span>
struct called_info <span class="token punctuation">{</span>
    u64 start<span class="token punctuation">;</span>  // <span class="token number">8</span>-byte
    u64 end<span class="token punctuation">;</span>    // <span class="token number">8</span>-byte
    u32 sector<span class="token punctuation">;</span> // <span class="token number">4</span>-byte
<span class="token punctuation">}</span><span class="token punctuation">;</span> // size of <span class="token number">20</span>-byte ?

printf<span class="token punctuation">(</span><span class="token string">&quot;size of %d-byte<span class="token entity" title="\\n">\\n</span>&quot;</span>, sizeof<span class="token punctuation">(</span>struct called_info<span class="token punctuation">))</span><span class="token punctuation">;</span> // size of <span class="token number">20</span>-byte

// Actual compiled composition of packed struct called_info
// 0x0<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span>                   0x8<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>
//  ↓________________________↓
//  <span class="token operator">|</span>        start <span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>       <span class="token operator">|</span>
//  <span class="token operator">|</span>________________________<span class="token operator">|</span>
//  <span class="token operator">|</span>         end  <span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>       <span class="token operator">|</span>
//  <span class="token operator">|</span>________________________<span class="token operator">|</span>
//  <span class="token operator">|</span>  sector<span class="token punctuation">(</span><span class="token number">4</span><span class="token punctuation">)</span> <span class="token operator">|</span>             <span class="token operator">&lt;=</span> address aligned to <span class="token number">4</span>
//  <span class="token operator">|</span>____________<span class="token operator">|</span>                 with no PADDING.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>通过将 <code>#pragma pack(4)</code> 定位在 <code>struct called_info</code> 之前，编译器会将结构体的成员对齐到至少 4 字节及其自然对齐方式。正如您所看到的， <code>struct called_info</code> 的大小已缩小为 20 字节，并且填充不再存在。</p><p>但是，去除填充也有缺点。例如，编译器将生成优化程度较低的代码。由于我们删除了填充，处理器将对结构进行未对齐的访问，这可能会导致性能下降。而且，在某些架构上，未对齐的访问可能会被验证者拒绝。</p><p>然而，有一种方法可以避免封装结构的缺点。只需在末尾添加显式填充 <code>u32 pad</code> 成员即可解决相同的问题，而无需打包结构。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>struct called_info <span class="token punctuation">{</span>
    u64 start<span class="token punctuation">;</span>  // <span class="token number">8</span>-byte
    u64 end<span class="token punctuation">;</span>    // <span class="token number">8</span>-byte
    u32 sector<span class="token punctuation">;</span> // <span class="token number">4</span>-byte
    u32 pad<span class="token punctuation">;</span>    // <span class="token number">4</span>-byte
<span class="token punctuation">}</span><span class="token punctuation">;</span> // size of <span class="token number">24</span>-byte ?

printf<span class="token punctuation">(</span><span class="token string">&quot;size of %d-byte<span class="token entity" title="\\n">\\n</span>&quot;</span>, sizeof<span class="token punctuation">(</span>struct called_info<span class="token punctuation">))</span><span class="token punctuation">;</span> // size of <span class="token number">24</span>-byte

// Actual compiled composition of struct called_info with explicit padding
// 0x0<span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span>                   0x8<span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>
//  ↓________________________↓
//  <span class="token operator">|</span>        start <span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>       <span class="token operator">|</span>
//  <span class="token operator">|</span>________________________<span class="token operator">|</span>
//  <span class="token operator">|</span>         end  <span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span>       <span class="token operator">|</span>
//  <span class="token operator">|</span>________________________<span class="token operator">|</span>
//  <span class="token operator">|</span>  sector<span class="token punctuation">(</span><span class="token number">4</span><span class="token punctuation">)</span> <span class="token operator">|</span>  pad <span class="token punctuation">(</span><span class="token number">4</span><span class="token punctuation">)</span>  <span class="token operator">|</span> <span class="token operator">&lt;=</span> address aligned to <span class="token number">8</span>
//  <span class="token operator">|</span>____________<span class="token operator">|</span>___________<span class="token operator">|</span>     with explicit PADDING.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="通过无效引用访问数据包数据" tabindex="-1"><a class="header-anchor" href="#通过无效引用访问数据包数据"><span>通过无效引用访问数据包数据</span></a></h4><p>某些网络 BPF 辅助函数（例如 <code>bpf_skb_store_bytes</code> ）可能会更改数据包数据的大小。由于验证者无法跟踪此类更改，因此验证者对数据的任何先验引用都将无效。因此，在访问数据之前需要更新参考，以避免验证者拒绝程序。</p><p>为了说明这一点，请考虑以下代码片段：</p><div class="language-c line-numbers-mode" data-ext="c" data-title="c"><pre class="language-c"><code><span class="token keyword">struct</span> <span class="token class-name">iphdr</span> <span class="token operator">*</span>ip4 <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token keyword">struct</span> <span class="token class-name">iphdr</span> <span class="token operator">*</span><span class="token punctuation">)</span> skb<span class="token operator">-&gt;</span>data <span class="token operator">+</span> ETH_HLEN<span class="token punctuation">;</span>

<span class="token function">skb_store_bytes</span><span class="token punctuation">(</span>skb<span class="token punctuation">,</span> l3_off <span class="token operator">+</span> <span class="token function">offsetof</span><span class="token punctuation">(</span><span class="token keyword">struct</span> <span class="token class-name">iphdr</span><span class="token punctuation">,</span> saddr<span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>new_saddr<span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>ip4<span class="token operator">-&gt;</span>protocol <span class="token operator">==</span> IPPROTO_TCP<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// do something</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>由于取消引用无效的 <code>ip4-&gt;protocol</code> ，验证程序将拒绝该代码段：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token assign-left variable">R1</span><span class="token operator">=</span>pkt_end<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span>,off<span class="token operator">=</span><span class="token number">0</span>,imm<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token assign-left variable">R2</span><span class="token operator">=</span>pkt<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span>,off<span class="token operator">=</span><span class="token number">34</span>,r<span class="token operator">=</span><span class="token number">34</span>,imm<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token assign-left variable">R3</span><span class="token operator">=</span>inv0
<span class="token assign-left variable">R6</span><span class="token operator">=</span>ctx<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span>,off<span class="token operator">=</span><span class="token number">0</span>,imm<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token assign-left variable">R7</span><span class="token operator">=</span>inv<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span>,umax_value<span class="token operator">=</span><span class="token number">4294967295</span>,var_off<span class="token operator">=</span><span class="token punctuation">(</span>0x0<span class="token punctuation">;</span> 0xffffffff<span class="token punctuation">))</span>
<span class="token assign-left variable">R8</span><span class="token operator">=</span>inv4294967162 <span class="token assign-left variable">R9</span><span class="token operator">=</span>pkt<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span>,off<span class="token operator">=</span><span class="token number">0</span>,r<span class="token operator">=</span><span class="token number">34</span>,imm<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token assign-left variable">R10</span><span class="token operator">=</span>fp0,call_-1
<span class="token punctuation">..</span>.
<span class="token number">18</span>: <span class="token punctuation">(</span><span class="token number">85</span><span class="token punctuation">)</span> call bpf_skb_store_bytes<span class="token comment">#9</span>
<span class="token number">19</span>: <span class="token punctuation">(</span>7b<span class="token punctuation">)</span> *<span class="token punctuation">(</span>u64 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r10 -56<span class="token punctuation">)</span> <span class="token operator">=</span> r7
<span class="token assign-left variable">R0</span><span class="token operator">=</span>inv<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token assign-left variable">R6</span><span class="token operator">=</span>ctx<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span>,off<span class="token operator">=</span><span class="token number">0</span>,imm<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token assign-left variable">R7</span><span class="token operator">=</span>inv<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span>,umax_value<span class="token operator">=</span><span class="token number">2</span>,var_off<span class="token operator">=</span><span class="token punctuation">(</span>0x0<span class="token punctuation">;</span> 0x3<span class="token punctuation">))</span>
<span class="token assign-left variable">R8</span><span class="token operator">=</span>inv4294967162 <span class="token assign-left variable">R9</span><span class="token operator">=</span>inv<span class="token punctuation">(</span>id<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token assign-left variable">R10</span><span class="token operator">=</span>fp0,call_-1 fp-48<span class="token operator">=</span>mmmm???? fp-56<span class="token operator">=</span>mmmmmmmm
<span class="token number">21</span>: <span class="token punctuation">(</span><span class="token number">61</span><span class="token punctuation">)</span> r1 <span class="token operator">=</span> *<span class="token punctuation">(</span>u32 *<span class="token punctuation">)</span><span class="token punctuation">(</span>r9 +23<span class="token punctuation">)</span>
R9 invalid mem access <span class="token string">&#39;inv&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>要解决此问题，必须更新对 <code>ip4</code> 的引用：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>struct iphdr *ip4 <span class="token operator">=</span> <span class="token punctuation">(</span>struct iphdr *<span class="token punctuation">)</span> skb-<span class="token operator">&gt;</span>data + ETH_HLEN<span class="token punctuation">;</span>

skb_store_bytes<span class="token punctuation">(</span>skb, l3_off + offsetof<span class="token punctuation">(</span>struct iphdr, saddr<span class="token punctuation">)</span>, <span class="token operator">&amp;</span>new_saddr, <span class="token number">4</span>, <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

ip4 <span class="token operator">=</span> <span class="token punctuation">(</span>struct iphdr *<span class="token punctuation">)</span> skb-<span class="token operator">&gt;</span>data + ETH_HLEN<span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>ip4-<span class="token operator">&gt;</span>protocol <span class="token operator">==</span> IPPROTO_TCP<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    // <span class="token keyword">do</span> something
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="iproute2" tabindex="-1"><a class="header-anchor" href="#iproute2"><span>iproute2</span></a></h2><p>有多种前端用于将 BPF 程序加载到内核中，例如 bcc、perf、iproute2 等。 Linux 内核源码树还在 <code>tools/lib/bpf/</code> 下提供了一个用户空间库，该库主要由 perf 使用和驱动，用于将 BPF 跟踪程序加载到内核中。然而，该库本身是通用的，不仅限于性能。 bcc 是一个工具包，提供许多有用的 BPF 程序，主要用于跟踪，这些程序通过嵌入 BPF C 代码的 Python 接口临时加载。不过，一般来说，前端之间实现 BPF 程序的语法和语义略有不同。此外，内核源代码树（ <code>samples/bpf/</code> ）中还有BPF示例，它们解析生成的目标文件并直接通过系统调用接口加载代码。</p><p>本节和前面的部分主要关注 iproute2 套件的 BPF 前端，用于加载 XDP、tc 或 lwt 类型的网络程序，因为 Cilium 的程序是针对此 BPF 加载器实现的。未来Cilium将配备原生BPF加载器，但程序仍将兼容通过iproute2套件加载，以方便开发和调试。</p><p>iproute2 支持的所有 BPF 程序类型共享相同的 BPF 加载器逻辑，因为具有作为库实现的公共加载器后端（iproute2 源代码树中的 <code>lib/bpf.c</code> ）。</p><p>上一节关于 LLVM 还介绍了一些与编写 BPF C 程序相关的 iproute2 部分，本文档的后面部分涉及编写程序时的 tc 和 XDP 特定方面。因此，本节将重点介绍使用 iproute2 加载目标文件的使用示例以及加载器的一些通用机制。它并不试图提供所有细节的完整覆盖，但足以入门。</p><h3 id="xdp-bpf-目标文件的加载" tabindex="-1"><a class="header-anchor" href="#xdp-bpf-目标文件的加载"><span>XDP BPF 目标文件的加载</span></a></h3><p>假设已为 XDP 编译了 BPF 目标文件 <code>prog.o</code> ，则可以使用以下命令通过 <code>ip</code> 将其加载到名为 <code>em1</code> 的支持 XDP 的网络设备：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> dev em1 xdp obj prog.o
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>上述命令假设程序代码位于默认部分，在 XDP 情况下称为 <code>prog</code> 。如果情况并非如此，并且该部分的名称不同，例如 <code>foobar</code> ，则需要将程序加载为：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> dev em1 xdp obj prog.o sec foobar
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>上述命令假设程序代码位于默认部分，在 XDP 情况下称为 <code>prog</code> 。如果情况并非如此，并且该部分的名称不同，例如 <code>foobar</code> ，则需要将程序加载为：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> dev em1 xdp obj prog.o sec foobar
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>请注意，也可以从 <code>.text</code> 部分加载程序。通过从 <code>xdp_drop</code> 入口点删除 <code>__section()</code> 注释来更改最小的独立 XDP 放置程序，如下所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#include &lt;linux/bpf.h&gt;</span>

<span class="token comment">#ifndef __section</span>
<span class="token comment"># define __section(NAME)                  \\</span>
   __attribute__<span class="token variable"><span class="token punctuation">((</span>section<span class="token punctuation">(</span>NAME<span class="token punctuation">)</span><span class="token punctuation">,</span> used<span class="token punctuation">))</span></span>
<span class="token comment">#endif</span>

int xdp_drop<span class="token punctuation">(</span>struct xdp_md *ctx<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    <span class="token builtin class-name">return</span> XDP_DROP<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

char __license<span class="token punctuation">[</span><span class="token punctuation">]</span> __section<span class="token punctuation">(</span><span class="token string">&quot;license&quot;</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token string">&quot;GPL&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>并且可以如下加载：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> dev em1 xdp obj prog.o sec .text
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>默认情况下，如果 XDP 程序已附加到网络接口， <code>ip</code> 将抛出错误，以防止它被意外覆盖。为了用新程序替换当前运行的 XDP 程序，必须使用 <code>-force</code> 选项：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">ip</span> <span class="token parameter variable">-force</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> dev em1 xdp obj prog.o
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如今，大多数支持 XDP 的驱动程序都支持用新程序原子替换现有程序，而不会中断流量。由于性能原因，始终只有一个程序附加到启用 XDP 的驱动程序，因此不支持程序链。然而，如上一节所述，必要时可以通过尾调用来执行程序分区，以实现类似的用例。</p><p>如果接口附加了 XDP 程序， <code>ip link</code> 命令将显示 <code>xdp</code> 标志。因此， <code>ip link | grep xdp</code> 可用于查找所有运行 XDP 的接口。通过 <code>ip -d link</code> 和 <code>bpftool</code> 的详细视图提供了进一步的自省功能，可用于根据 <code>ip link</code> 转储。</p><p>为了从接口中删除现有的 XDP 程序，必须发出以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code>$ <span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> dev em1 xdp off
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>在将驱动程序的操作模式从非 XDP 切换到本机 XDP 的情况下，反之亦然，驱动程序通常需要重新配置其接收（和发送）环，以确保接收到的数据包在单页内线性设置，以便 BPF读取并写入。然而，一旦完成，大多数驱动程序只需要在请求交换 BPF 程序时执行程序本身的原子替换。</p><p>总的来说，XDP 支持 iproute2 实现的三种操作模式： <code>xdpdrv</code> 、 <code>xdpoffload</code> 和 <code>xdpgeneric</code> 。</p><p><code>xdpdrv</code> 代表本机 XDP，这意味着 BPF 程序最早在软件中直接在驱动程序的接收路径中运行。这是正常/传统的 XDP 模式，需要驱动程序来实现 XDP 支持，上游 Linux 内核中的所有主要 10G/40G/+ 网络驱动程序都已提供。</p><p><code>xdpgeneric</code> 代表通用 XDP，旨在作为尚不支持本机 XDP 的驱动程序的实验测试台。鉴于入口路径中的通用 XDP 挂钩出现的时间要晚得多，此时数据包已经作为 <code>skb</code> 进入堆栈的主接收路径，因此性能明显低于 <code>xdpdrv</code> 因此，大多数情况下仅对实验感兴趣，对生产环境较少。</p><p>最后但并非最不重要的一点是， <code>xdpoffload</code> 模式由SmartNIC实现，例如Netronome的nfp驱动程序支持的那些，并允许将整个BPF/XDP程序卸载到硬件中，因此该程序在每个数据包接收时直接在卡片。这提供了比在本机 XDP 中运行更高的性能，尽管与本机 XDP 相比，并非所有 BPF 映射类型或 BPF 辅助函数都可供使用。在这种情况下，BPF 验证者将拒绝该程序并向用户报告不支持的内容。除了停留在受支持的 BPF 功能和辅助函数范围内之外，在编写 BPF C 程序时无需采取特殊的预防措施。</p><p>当使用像 <code>ip link set dev em1 xdp obj [...]</code> 这样的命令时，内核将尝试首先将程序作为本机 XDP 加载，如果驱动程序不支持本机 XDP，它将自动回退到通用 XDP。因此，例如，显式使用 <code>xdpdrv</code> 而不是 <code>xdp</code> ，内核只会尝试将程序加载为本机 XDP，并在驱动程序不支持它的情况下失败，这提供了保证完全避免通用 XDP。</p><p>强制以本机 XDP 模式加载 BPF/XDP 程序、转储链接详细信息并再次卸载程序的示例：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ip -force link set dev em1 xdpdrv obj prog.o</span>
<span class="token comment"># ip link show</span>
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token number">6</span>: em1: <span class="token operator">&lt;</span>BROADCAST,MULTICAST,UP,LOWER_UP<span class="token operator">&gt;</span> mtu <span class="token number">1500</span> xdp qdisc mq state UP mode DORMANT group default qlen <span class="token number">1000</span>
    link/ether be:08:4d:b6:85:65 brd ff:ff:ff:ff:ff:ff
    prog/xdp <span class="token function">id</span> <span class="token number">1</span> tag 57cd311f2e27366b
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token comment"># ip link set dev em1 xdpdrv off</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>现在使用相同的示例强制通用 XDP，即使驱动程序支持本机 XDP，并另外通过 bpftool 转储附加虚拟程序的 BPF 指令：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ip -force link set dev em1 xdpgeneric obj prog.o</span>
<span class="token comment"># ip link show</span>
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token number">6</span>: em1: <span class="token operator">&lt;</span>BROADCAST,MULTICAST,UP,LOWER_UP<span class="token operator">&gt;</span> mtu <span class="token number">1500</span> xdpgeneric qdisc mq state UP mode DORMANT group default qlen <span class="token number">1000</span>
    link/ether be:08:4d:b6:85:65 brd ff:ff:ff:ff:ff:ff
    prog/xdp <span class="token function">id</span> <span class="token number">4</span> tag 57cd311f2e27366b                <span class="token operator">&lt;</span>-- BPF program ID <span class="token number">4</span>
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token comment"># bpftool prog dump xlated id 4                       &lt;-- Dump of instructions running on em1</span>
<span class="token number">0</span>: <span class="token punctuation">(</span>b7<span class="token punctuation">)</span> r0 <span class="token operator">=</span> <span class="token number">1</span>
<span class="token number">1</span>: <span class="token punctuation">(</span><span class="token number">95</span><span class="token punctuation">)</span> <span class="token builtin class-name">exit</span>
<span class="token comment"># ip link set dev em1 xdpgeneric off</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>最后但并非最不重要的卸载 XDP，我们还通过 bpftool 转储程序信息以检索一般元数据：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ip -force link set dev em1 xdpoffload obj prog.o</span>
<span class="token comment"># ip link show</span>
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token number">6</span>: em1: <span class="token operator">&lt;</span>BROADCAST,MULTICAST,UP,LOWER_UP<span class="token operator">&gt;</span> mtu <span class="token number">1500</span> xdpoffload qdisc mq state UP mode DORMANT group default qlen <span class="token number">1000</span>
    link/ether be:08:4d:b6:85:65 brd ff:ff:ff:ff:ff:ff
    prog/xdp <span class="token function">id</span> <span class="token number">8</span> tag 57cd311f2e27366b
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token comment"># bpftool prog show id 8</span>
<span class="token number">8</span>: xdp  tag 57cd311f2e27366b dev em1                  <span class="token operator">&lt;</span>-- Also indicates a BPF program offloaded to em1
    loaded_at Apr <span class="token number">11</span>/20:38  uid <span class="token number">0</span>
    xlated 16B  not jited  memlock 4096B
<span class="token comment"># ip link set dev em1 xdpoffload off</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>请注意，不可能同时使用 <code>xdpdrv</code> 和 <code>xdpgeneric</code> 或其他模式，这意味着只能选择一种 XDP 操作模式。</p><p>不同 XDP 模式之间的切换，例如从通用到本机或反之亦然在原子上是不可能的。仅在特定操作模式下切换程序：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ip -force link set dev em1 xdpgeneric obj prog.o</span>
<span class="token comment"># ip -force link set dev em1 xdpoffload obj prog.o</span>
RTNETLINK answers: File exists
<span class="token comment"># ip -force link set dev em1 xdpdrv obj prog.o</span>
RTNETLINK answers: File exists
<span class="token comment"># ip -force link set dev em1 xdpgeneric obj prog.o    &lt;-- Succeeds due to xdpgeneric</span>
<span class="token comment">#</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>模式之间的切换需要先离开当前的操作模式，然后才能进入新的操作模式：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ip -force link set dev em1 xdpgeneric obj prog.o</span>
<span class="token comment"># ip -force link set dev em1 xdpgeneric off</span>
<span class="token comment"># ip -force link set dev em1 xdpoffload obj prog.o</span>
<span class="token comment"># ip l</span>
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token number">6</span>: em1: <span class="token operator">&lt;</span>BROADCAST,MULTICAST,UP,LOWER_UP<span class="token operator">&gt;</span> mtu <span class="token number">1500</span> xdpoffload qdisc mq state UP mode DORMANT group default qlen <span class="token number">1000</span>
    link/ether be:08:4d:b6:85:65 brd ff:ff:ff:ff:ff:ff
    prog/xdp <span class="token function">id</span> <span class="token number">17</span> tag 57cd311f2e27366b
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token comment"># ip -force link set dev em1 xdpoffload off</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="加载-tc-bpf-目标文件" tabindex="-1"><a class="header-anchor" href="#加载-tc-bpf-目标文件"><span>加载 tc BPF 目标文件</span></a></h3><p>假设已经为 tc 编译了 BPF 目标文件 <code>prog.o</code> ，则可以通过 tc 命令将其加载到网络设备。与 XDP 不同，不存在支持将 BPF 程序附加到设备的驱动程序依赖性。这里，网络设备称为 <code>em1</code> ，使用以下命令可以将程序附加到 <code>em1</code> 的网络 <code>ingress</code> 路径：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># tc qdisc add dev em1 clsact</span>
<span class="token comment"># tc filter add dev em1 ingress bpf da obj prog.o</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>第一步是设置 <code>clsact</code> qdisc（Linux 排队规则）。 <code>clsact</code> 是一个类似于 <code>ingress</code> qdisc的虚拟qdisc，它只能保存分类器和动作，但不执行实际的排队。为了附加 <code>bpf</code> 分类器，需要它。 <code>clsact</code> qdisc 提供了两个名为 <code>ingress</code> 和 <code>egress</code> 的特殊钩子，可以将分类器附加到其中。 <code>ingress</code> 和 <code>egress</code> 挂钩都位于网络数据路径中的中央接收和传输位置，设备上的每个数据包都经过这里。 <code>ingress</code> 钩子是从内核中的 <code>__netif_receive_skb_core() -&gt; sch_handle_ingress()</code> 调用的， <code>egress</code> 钩子是从 <code>__dev_queue_xmit() -&gt; sch_handle_egress()</code> 调用的。</p><p>将程序附加到 <code>egress</code> 挂钩的等效操作如下所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># tc filter add dev em1 egress bpf da obj prog.o</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><code>clsact</code> qdisc 从 <code>ingress</code> 和 <code>egress</code> 方向进行无锁处理，并且还可以附加到虚拟的无队列设备，例如 <code>veth</code> 连接容器的设备。</p><p>在挂钩旁边， <code>tc filter</code> 命令选择要在 <code>da</code> （直接操作）模式下使用的 <code>bpf</code> 。建议使用 <code>da</code> 模式，并且应始终指定该模式。它基本上意味着 <code>bpf</code> 分类器不需要调用外部 tc 操作模块，这对于 <code>bpf</code> 来说并不是必需的，因为所有数据包修改、转发或其他类型的操作都可以已经在要附加的单个 BPF 程序内执行，因此速度明显更快。</p><p>此时，程序已被附加，并在数据包穿过设备后执行。就像在 XDP 中一样，如果不使用默认的节名称，则可以在加载期间指定它，例如，在节 <code>foobar</code> 的情况下：</p><p>iproute2 的 BPF 加载器允许跨程序类型使用相同的命令行语法，因此 <code>obj prog.o sec foobar</code> 与前面提到的 XDP 的语法相同。</p><p>可以通过以下命令列出附加程序：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># tc filter show dev em1 ingress</span>
filter protocol all pref <span class="token number">49152</span> bpf
filter protocol all pref <span class="token number">49152</span> bpf handle 0x1 prog.o:<span class="token punctuation">[</span>ingress<span class="token punctuation">]</span> direct-action <span class="token function">id</span> <span class="token number">1</span> tag c5f7825e5dac396f

<span class="token comment"># tc filter show dev em1 egress</span>
filter protocol all pref <span class="token number">49152</span> bpf
filter protocol all pref <span class="token number">49152</span> bpf handle 0x1 prog.o:<span class="token punctuation">[</span>egress<span class="token punctuation">]</span> direct-action <span class="token function">id</span> <span class="token number">2</span> tag b2fd5adc0f262714
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>prog.o:[ingress]</code> 的输出表明程序段 <code>ingress</code> 是从文件 <code>prog.o</code> 加载的，并且 <code>bpf</code> 在 <code>direct-action</code> 模式。为每种情况附加程序 <code>id</code> 和 <code>tag</code> ，其中后者表示指令流上的散列，可以与目标文件或 <code>perf</code> 报告相关联最后但并非最不重要的一点是， <code>id</code> 表示系统范围内唯一的 BPF 程序标识符，可以与 <code>bpftool</code> 一起使用来进一步检查或转储附加的 BPF 程序。</p><p>tc 不仅可以附加一个 BPF 程序，它还提供可以链接在一起的各种其他分类器。然而，附加单个 BPF 程序就足够了，因为借助 <code>da</code> ( <code>direct-action</code> ) 模式，所有数据包操作都可以包含在程序本身中，这意味着 BPF 程序本身已经返回tc 操作判决，例如 <code>TC_ACT_OK</code> 、 <code>TC_ACT_SHOT</code> 等。为了获得最佳性能和灵活性，这是推荐的用法。</p><p>在上面的 <code>show</code> 命令中，tc 还在 BPF 相关输出旁边显示 <code>pref 49152</code> 和 <code>handle 0x1</code> 。如果未通过命令行显式提供，两者都会自动生成。 <code>pref</code> 表示优先级编号，这意味着如果附加了多个分类器，它们将根据升序优先级执行， <code>handle</code> 表示一个标识符，如果同一分类器有多个实例已加载到相同的 <code>pref</code> 下。由于在 BPF 的情况下，单个程序就完全足够了，因此通常可以忽略 <code>pref</code> 和 <code>handle</code> 。</p><p>仅在计划以原子方式替换附加的 BPF 程序的情况下，建议在初始加载时显式指定先验 <code>pref</code> 和 <code>handle</code> ，以便它们不具有稍后查询 <code>replace</code> 操作。因此，创造就变成了：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># tc filter add dev em1 ingress pref 1 handle 1 bpf da obj prog.o sec foobar</span>

<span class="token comment"># tc filter show dev em1 ingress</span>
filter protocol all pref <span class="token number">1</span> bpf
filter protocol all pref <span class="token number">1</span> bpf handle 0x1 prog.o:<span class="token punctuation">[</span>foobar<span class="token punctuation">]</span> direct-action <span class="token function">id</span> <span class="token number">1</span> tag c5f7825e5dac396f
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>对于原子替换，可以发出以下命令，用 <code>foobar</code> 部分中的文件 <code>prog.o</code> 中的新 BPF 程序更新 <code>ingress</code> 钩子处的现有程序：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># tc filter replace dev em1 ingress pref 1 handle 1 bpf da obj prog.o sec foobar</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>最后但并非最不重要的一点是，为了从 <code>ingress</code> 和 <code>egress</code> 挂钩中删除所有附加程序，可以使用以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># tc filter del dev em1 ingress</span>
<span class="token comment"># tc filter del dev em1 egress</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>为了从网络设备中删除整个 <code>clsact</code> qdisc，这也隐式地从 <code>ingress</code> 和 <code>egress</code> 挂钩中删除所有附加的程序，提供了以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># tc qdisc del dev em1 clsact</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果 NIC 和驱动程序支持它，则也可以卸载 tc BPF 程序，与 XDP BPF 程序类似。 Netronome 的 nfp 支持的 NIC 提供两种类型的 BPF 卸载。</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># tc qdisc add dev em1 clsact</span>
<span class="token comment"># tc filter replace dev em1 ingress pref 1 handle 1 bpf skip_sw da obj prog.o</span>
Error: TC offload is disabled on net device.
We have an error talking to the kernel
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如果显示上述错误，则首先需要通过 ethtool 的 <code>hw-tc-offload</code> 设置为设备启用 tc hardware offload：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ethtool -K em1 hw-tc-offload on</span>
<span class="token comment"># tc qdisc add dev em1 clsact</span>
<span class="token comment"># tc filter replace dev em1 ingress pref 1 handle 1 bpf skip_sw da obj prog.o</span>
<span class="token comment"># tc filter show dev em1 ingress</span>
filter protocol all pref <span class="token number">1</span> bpf
filter protocol all pref <span class="token number">1</span> bpf handle 0x1 prog.o:<span class="token punctuation">[</span>classifier<span class="token punctuation">]</span> direct-action skip_sw in_hw <span class="token function">id</span> <span class="token number">19</span> tag 57cd311f2e27366b
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>in_hw</code> 标志确认程序已卸载到 NIC。</p><p>请注意，tc 和 XDP 的 BPF 卸载不能同时加载，必须选择 tc 或 XDP 卸载选项。</p><h3 id="通过-netdevsim-驱动程序测试-bpf-卸载接口" tabindex="-1"><a class="header-anchor" href="#通过-netdevsim-驱动程序测试-bpf-卸载接口"><span>通过 netdevsim 驱动程序测试 BPF 卸载接口</span></a></h3><p>netdevsim 驱动程序是 Linux 内核的一部分，提供了一个虚拟驱动程序，该驱动程序实现了 XDP BPF 和 tc BPF 程序的卸载接口，并有助于测试内核更改或直接针对内核 UAPI 实现控制平面的低级用户空间程序。</p><p>可以按如下方式创建 netdevsim 设备：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># modprobe netdevsim</span>
// <span class="token punctuation">[</span>ID<span class="token punctuation">]</span> <span class="token punctuation">[</span>PORT_COUNT<span class="token punctuation">]</span>
<span class="token comment"># echo &quot;1 1&quot; &gt; /sys/bus/netdevsim/new_device</span>
<span class="token comment"># devlink dev</span>
netdevsim/netdevsim1
<span class="token comment"># devlink port</span>
netdevsim/netdevsim1/0: <span class="token builtin class-name">type</span> eth netdev eth0 flavour physical
<span class="token comment"># ip l</span>
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token number">4</span>: eth0: <span class="token operator">&lt;</span>BROADCAST,NOARP,UP,LOWER_UP<span class="token operator">&gt;</span> mtu <span class="token number">1500</span> qdisc noqueue state UNKNOWN mode DEFAULT group default qlen <span class="token number">1000</span>
    link/ether 2a:d5:cd:08:d1:3f brd ff:ff:ff:ff:ff:ff
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>完成该步骤后，可以测试加载 XDP BPF 或 tc BPF 程序，如前面的各个示例所示：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ip -force link set dev eth0 xdpoffload obj prog.o</span>
<span class="token comment"># ip l</span>
<span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span>
<span class="token number">4</span>: eth0: <span class="token operator">&lt;</span>BROADCAST,NOARP,UP,LOWER_UP<span class="token operator">&gt;</span> mtu <span class="token number">1500</span> xdpoffload qdisc noqueue state UNKNOWN mode DEFAULT group default qlen <span class="token number">1000</span>
    link/ether 2a:d5:cd:08:d1:3f brd ff:ff:ff:ff:ff:ff
    prog/xdp <span class="token function">id</span> <span class="token number">16</span> tag a04f5eef06a7f555
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这两个工作流程是使用 iproute2 分别加载 XDP BPF 和 tc BPF 程序的基本操作。</p><p>BPF 加载器还有其他各种高级选项，适用于 XDP 和 tc，此处列出了其中一些。为了简单起见，在示例中仅提供了 XDP。</p><ol><li><h4 id="即使成功也会输出详细的日志" tabindex="-1"><a class="header-anchor" href="#即使成功也会输出详细的日志"><span>即使成功也会输出详细的日志</span></a></h4></li></ol><p>可以附加选项 <code>verb</code> 来加载程序，以便转储验证程序日志，即使没有发生错误：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ip link set dev em1 xdp obj xdp-example.o verb </span>

Prog section <span class="token string">&#39;prog&#39;</span> loaded <span class="token punctuation">(</span><span class="token number">5</span><span class="token punctuation">)</span><span class="token operator">!</span>
 - Type:         <span class="token number">6</span>
 - Instructions: <span class="token number">2</span> <span class="token punctuation">(</span><span class="token number">0</span> over limit<span class="token punctuation">)</span>
 - License:      GPL

Verifier analysis:

<span class="token number">0</span>: <span class="token punctuation">(</span>b7<span class="token punctuation">)</span> r0 <span class="token operator">=</span> <span class="token number">1</span>
<span class="token number">1</span>: <span class="token punctuation">(</span><span class="token number">95</span><span class="token punctuation">)</span> <span class="token builtin class-name">exit</span>
processed <span class="token number">2</span> insns
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ol start="2"><li><h4 id="加载已固定在-bpf-文件系统中的程序" tabindex="-1"><a class="header-anchor" href="#加载已固定在-bpf-文件系统中的程序"><span>加载已固定在 BPF 文件系统中的程序</span></a></h4></li></ol><p>iproute2 不是从目标文件加载程序，而是可以从 BPF 文件系统检索程序，以防某些外部实体将其固定在那里并将其附加到设备：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># ip link set dev em1 xdp pinned /sys/fs/bpf/prog</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>iproute2 还可以使用与检测到的 BPF 文件系统挂载点相关的缩写形式：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> dev em1 xdp pinned m:prog
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>当加载BPF程序时，iproute2会自动检测已挂载的文件系统实例，以便执行节点的固定。如果没有找到已挂载的 BPF 文件系统实例，则 tc 会自动将其挂载到 <code>/sys/fs/bpf/</code> 下的默认位置。</p><p>如果已经找到实例，则将使用该实例并且不会执行额外的安装：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment"># mkdir /var/run/bpf</span>
<span class="token comment"># mount --bind /var/run/bpf /var/run/bpf</span>
<span class="token comment"># mount -t bpf bpf /var/run/bpf</span>
<span class="token comment"># tc filter add dev em1 ingress bpf da obj tc-example.o sec prog</span>
<span class="token comment"># tree /var/run/bpf</span>
/var/run/bpf
+-- <span class="token function">ip</span> -<span class="token operator">&gt;</span> /run/bpf/tc/
+-- tc
<span class="token operator">|</span>   +-- globals
<span class="token operator">|</span>       +-- jmp_map
+-- xdp -<span class="token operator">&gt;</span> /run/bpf/tc/

<span class="token number">4</span> directories, <span class="token number">1</span> <span class="token function">file</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>默认情况下，tc 将创建一个如上所示的初始目录结构，其中所有子系统用户将通过 <code>globals</code> 命名空间的符号链接指向同一位置，以便固定的 BPF 映射可以在各种 BPF 程序类型之间重用在iproute2中。如果文件系统实例已被安装并且现有结构已存在，则 tc 将不会覆盖它。这可能是分离 <code>lwt</code> 、 <code>tc</code> 和 <code>xdp</code> 映射的情况，以便不在所有映射之间共享 <code>globals</code> 。</p><p>正如前面的 LLVM 部分简要介绍的那样，iproute2 将在安装时安装一个头文件，BPF 程序可以通过标准包含路径包含该头文件：</p><div class="language-bash line-numbers-mode" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token comment">#include &lt;iproute2/bpf_elf.h&gt;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>该头文件的目的是为程序使用的映射和默认节名称提供 API。它是 iproute2 和 BPF 程序之间的稳定契约。</p><p>iproute2 的映射定义是 <code>struct bpf_elf_map</code> 。其成员已在本文档前面的 LLVM 部分介绍过。</p><p>当解析 BPF 目标文件时，iproute2 加载器将遍历所有 ELF 部分。它最初获取 <code>maps</code> 和 <code>license</code> 等辅助部分。对于 <code>maps</code> ，将检查 <code>struct bpf_elf_map</code> 数组的有效性，并在需要时执行兼容性解决方法。随后，所有地图都使用用户提供的信息创建，或者作为固定对象检索，或者新创建然后固定到 BPF 文件系统中。接下来，加载器将处理所有包含 ELF 映射重定位条目的程序段，这意味着将映射文件描述符加载到寄存器中的 BPF 指令将被重写，以便相应的映射文件描述符被编码到指令立即值中，以便内核能够稍后能够将它们转换为映射内核指针。之后，所有程序本身都通过 BPF 系统调用创建，并且尾部调用映射（如果存在），并使用程序的文件描述符进行更新。</p>`,279);function b(k,_){const a=c("ExternalLinkIcon");return l(),i("div",null,[d,s("blockquote",null,[r,s("p",null,[n("对于内核版本 4.16+，BPF 自测试依赖于 LLVM 6.0+，这是由不再需要内联的 BPF 函数调用引起的。有关更多信息，请参阅 "),s("a",u,[n("BPF to BPF Calls"),e(a)]),n(" 部分或来自内核补丁的求职信邮件 (https://lwn.net/Articles/741773/)。如果不使用此新功能，则并非每个 BPF 程序都依赖于 LLVM 6.0+。如果您的发行版不提供 LLVM 6.0+，您可以按照 "),s("a",m,[n("LLVM"),e(a)]),n(" 部分中的说明进行编译。")])]),v])}const h=t(o,[["render",b],["__file","03. 开发工具.html.vue"]]),P=JSON.parse('{"path":"/MdNote_Public/01.%20%E8%AE%BE%E8%AE%A1%E5%BC%80%E5%8F%91%E4%B8%8E%E6%95%B0%E6%8D%AE%E7%94%9F%E4%BA%A7/Develop/02.%20Theory/Computer/03.%20%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%20-%20%E4%B8%93%E9%A2%98%E6%88%96%E5%AD%90%E7%B3%BB%E7%BB%9F%E7%9A%84%E5%AD%97%E5%85%B8%E7%89%88/%E4%B8%8B%E5%B1%82%E7%9B%B8%E5%85%B3/Network/%E7%BD%91%E7%BB%9C%E5%BA%93/%E6%97%A0%E5%8D%8F%E8%AE%AE%E6%A0%88%E5%BA%93/Cilium%20(eBPF)/%E5%AE%98%E6%96%B9%E6%96%87%E6%A1%A3/10.%20BPF%E5%92%8CXDP%E5%8F%82%E8%80%83%E6%8C%87%E5%8D%97/03.%20%E5%BC%80%E5%8F%91%E5%B7%A5%E5%85%B7.html","title":"eBPF","lang":"zh-CN","frontmatter":{"description":"eBPF 目录 开发工具 本节讨论 BPF 周围的当前用户空间工具、内省工具和内核控制旋钮。 !NOTE BPF 周围的工具和基础设施仍在快速发展，因此可能无法提供所有可用工具的完整情况。 开发环境 下面可以找到针对 Fedora 和 Ubuntu 设置 BPF 开发环境的分步指南。这将指导您构建、安装和测试开发内核以及构建和安装 iproute2。 ...","head":[["meta",{"property":"og:url","content":"http://192.168.0.101:8080/MdNote_Public/01.%20%E8%AE%BE%E8%AE%A1%E5%BC%80%E5%8F%91%E4%B8%8E%E6%95%B0%E6%8D%AE%E7%94%9F%E4%BA%A7/Develop/02.%20Theory/Computer/03.%20%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%20-%20%E4%B8%93%E9%A2%98%E6%88%96%E5%AD%90%E7%B3%BB%E7%BB%9F%E7%9A%84%E5%AD%97%E5%85%B8%E7%89%88/%E4%B8%8B%E5%B1%82%E7%9B%B8%E5%85%B3/Network/%E7%BD%91%E7%BB%9C%E5%BA%93/%E6%97%A0%E5%8D%8F%E8%AE%AE%E6%A0%88%E5%BA%93/Cilium%20(eBPF)/%E5%AE%98%E6%96%B9%E6%96%87%E6%A1%A3/10.%20BPF%E5%92%8CXDP%E5%8F%82%E8%80%83%E6%8C%87%E5%8D%97/03.%20%E5%BC%80%E5%8F%91%E5%B7%A5%E5%85%B7.html"}],["meta",{"property":"og:site_name","content":"Linc 的小站"}],["meta",{"property":"og:title","content":"eBPF"}],["meta",{"property":"og:description","content":"eBPF 目录 开发工具 本节讨论 BPF 周围的当前用户空间工具、内省工具和内核控制旋钮。 !NOTE BPF 周围的工具和基础设施仍在快速发展，因此可能无法提供所有可用工具的完整情况。 开发环境 下面可以找到针对 Fedora 和 Ubuntu 设置 BPF 开发环境的分步指南。这将指导您构建、安装和测试开发内核以及构建和安装 iproute2。 ..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"article:author","content":"LincZero"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"eBPF\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"LincZero\\",\\"url\\":\\"https://github.com/LincZero/\\"}]}"]]},"headers":[{"level":1,"title":"eBPF","slug":"ebpf","link":"#ebpf","children":[]},{"level":1,"title":"目录","slug":"目录","link":"#目录","children":[]},{"level":1,"title":"开发工具","slug":"开发工具","link":"#开发工具","children":[{"level":2,"title":"开发环境","slug":"开发环境","link":"#开发环境","children":[{"level":3,"title":"Fedora","slug":"fedora","link":"#fedora","children":[]},{"level":3,"title":"Ubuntu","slug":"ubuntu","link":"#ubuntu","children":[]},{"level":3,"title":"OpenSUSE Tumbleweed","slug":"opensuse-tumbleweed","link":"#opensuse-tumbleweed","children":[]}]},{"level":2,"title":"编译内核","slug":"编译内核","link":"#编译内核","children":[{"level":3,"title":"验证设置","slug":"验证设置","link":"#验证设置","children":[]},{"level":3,"title":"编译 iproute2","slug":"编译-iproute2","link":"#编译-iproute2","children":[]},{"level":3,"title":"编译 bpftool","slug":"编译-bpftool","link":"#编译-bpftool","children":[]}]},{"level":2,"title":"LLVM","slug":"llvm","link":"#llvm","children":[{"level":3,"title":"LLVM","slug":"llvm-1","link":"#llvm-1","children":[]},{"level":3,"title":"BPF 模型的一些差异","slug":"bpf-模型的一些差异","link":"#bpf-模型的一些差异","children":[{"level":4,"title":"一切都需要内联，没有可用的函数调用（在旧的 LLVM 版本上）或共享库调用","slug":"一切都需要内联-没有可用的函数调用-在旧的-llvm-版本上-或共享库调用","link":"#一切都需要内联-没有可用的函数调用-在旧的-llvm-版本上-或共享库调用","children":[]},{"level":4,"title":"多个程序可以驻留在单个 C 文件的不同部分中","slug":"多个程序可以驻留在单个-c-文件的不同部分中","link":"#多个程序可以驻留在单个-c-文件的不同部分中","children":[]},{"level":4,"title":"不允许使用全局变量","slug":"不允许使用全局变量","link":"#不允许使用全局变量","children":[]},{"level":4,"title":"不允许使用 const 字符串或数组","slug":"不允许使用-const-字符串或数组","link":"#不允许使用-const-字符串或数组","children":[]},{"level":4,"title":"使用 LLVM 内置函数 memset()/memcpy()/memmove()/memcmp()","slug":"使用-llvm-内置函数-memset-memcpy-memmove-memcmp","link":"#使用-llvm-内置函数-memset-memcpy-memmove-memcmp","children":[]},{"level":4,"title":"目前还没有可用的循环","slug":"目前还没有可用的循环","link":"#目前还没有可用的循环","children":[]},{"level":4,"title":"使用尾调用对程序进行分区","slug":"使用尾调用对程序进行分区","link":"#使用尾调用对程序进行分区","children":[]},{"level":4,"title":"堆栈空间有限，最多 512 字节。","slug":"堆栈空间有限-最多-512-字节。","link":"#堆栈空间有限-最多-512-字节。","children":[]},{"level":4,"title":"可以使用 BPF 内联组装","slug":"可以使用-bpf-内联组装","link":"#可以使用-bpf-内联组装","children":[]},{"level":4,"title":"使用 #pragma pack 删除结构填充并对齐成员","slug":"使用-pragma-pack-删除结构填充并对齐成员","link":"#使用-pragma-pack-删除结构填充并对齐成员","children":[]},{"level":4,"title":"通过无效引用访问数据包数据","slug":"通过无效引用访问数据包数据","link":"#通过无效引用访问数据包数据","children":[]}]}]},{"level":2,"title":"iproute2","slug":"iproute2","link":"#iproute2","children":[{"level":3,"title":"XDP BPF 目标文件的加载","slug":"xdp-bpf-目标文件的加载","link":"#xdp-bpf-目标文件的加载","children":[]},{"level":3,"title":"加载 tc BPF 目标文件","slug":"加载-tc-bpf-目标文件","link":"#加载-tc-bpf-目标文件","children":[]},{"level":3,"title":"通过 netdevsim 驱动程序测试 BPF 卸载接口","slug":"通过-netdevsim-驱动程序测试-bpf-卸载接口","link":"#通过-netdevsim-驱动程序测试-bpf-卸载接口","children":[]}]}]}],"git":{"createdTime":null,"updatedTime":null,"contributors":[]},"readingTime":{"minutes":50.79,"words":15238},"filePathRelative":"MdNote_Public/01. 设计开发与数据生产/Develop/02. Theory/Computer/03. 计算机系统 - 专题或子系统的字典版/下层相关/Network/网络库/无协议栈库/Cilium (eBPF)/官方文档/10. BPF和XDP参考指南/03. 开发工具.md","autoDesc":true}');export{h as comp,P as data};
